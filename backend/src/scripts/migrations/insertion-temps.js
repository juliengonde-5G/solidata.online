/**
 * Migration PR B — lot 4 « Temps d'accompagnement ». SQUELETTE : le lot 4 le
 * remplit selon le contrat 15 § 3. Idempotent, client.query uniquement.
 */
async function run(client) {
  console.log('[INIT-DB] Migration insertion-temps (PR B lot 4) : squelette, rien à faire ✓');
}

module.exports = { run };
