// Squelette posé par l'orchestrateur (PR C 2.54.0) — contrat 20 § 6 (lot 7) :
// impressions A4 « Mon parcours en une page » (FALC, UNE page) et « Mon Récap »,
// rendues EXCLUSIVEMENT depuis l'objet `contenu` composé par le serveur.
export function exportMonParcoursPDF(_contenu) {}
export function exportMonRecapPDF(_contenu) {}
