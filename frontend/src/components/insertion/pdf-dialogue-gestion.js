// Squelette posé par l'orchestrateur (PR D 2.55.0) — contrat 25 § 6 : PDF A4 de la synthèse (9 blocs + page Méthode),
// rendu EXCLUSIVEMENT depuis l'objet `contenu` composé par le serveur.
export function exportDialogueGestionPDF(_contenu) {}
