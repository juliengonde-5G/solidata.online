# SOLIDATA ERP — Instructions pour Agents IA

> **Ce fichier est le contexte de référence pour tout agent IA (Claude, Copilot, etc.) travaillant sur le projet SOLIDATA.**
> Il est lu automatiquement par Claude Code au démarrage de chaque session.
> Dernière mise à jour : 13 juillet 2026

---

## 1. IDENTITE DU PROJET

**SOLIDATA** est un ERP (Enterprise Resource Planning) conçu pour **Solidarité Textiles**, une structure d'insertion par l'activité économique (SIAE/IAE) spécialisée dans la collecte, le tri et la valorisation de textiles usagés en Normandie (Rouen).

- **Domaine** : https://solidata.online
- **Mobile** : https://m.solidata.online
- **Dépôt** : https://github.com/juliengonde-5G/solidata.online
- **Branche principale** : `main`
- **Serveur production** : Scaleway DEV1-S, IP 51.159.144.100
- **Répertoire serveur** : `/opt/solidata.online`

---

## 2. STACK TECHNIQUE

| Couche | Technologies |
|--------|-------------|
| **Backend** | Node.js 20, Express 4.21, PostgreSQL 15 + PostGIS 3.4, Redis 7, Socket.IO 4.8, BullMQ |
| **Frontend Web** | React 18.3, Vite 6, Tailwind CSS 3.4, React Router 7, Recharts, Leaflet |
| **Mobile PWA** | React 18.3, Vite + vite-plugin-pwa, Tailwind, html5-qrcode, Socket.IO |
| **Infrastructure** | Docker Compose (7 conteneurs), Nginx reverse proxy, Let's Encrypt SSL, UFW, Fail2ban |
| **Sécurité** | JWT (access 8h + refresh 7j), bcrypt, AES-256 (PCM), rate limiting, HSTS |
| **IA/ML** | Moteur prédictif remplissage CAV, optimisation tournées, insertion IA |

---

## 3. ARCHITECTURE DES CONTENEURS

```
Internet → Nginx SSL (:443)
              ├── solidata.online     → solidata-web (React, :80)
              ├── m.solidata.online   → solidata-mobile (PWA, :80)
              ├── /api/*              → solidata-api (Node.js, :3001)
              ├── /socket.io/*        → solidata-api (WebSocket)
              └── /uploads/*          → solidata-api (fichiers)
                                           ├── solidata-db (PostgreSQL+PostGIS, :5432)
                                           └── solidata-redis (Redis 7, :6379)
```

---

## 4. STRUCTURE DU CODE

```
solidata.online/
├── backend/src/
│   ├── index.js              # Entry point Express + Socket.IO + auto-init DB
│   ├── config/database.js    # Pool PostgreSQL
│   ├── middleware/auth.js     # authenticate() + authorize(...roles)
│   ├── routes/               # 69 fichiers de routes API
│   ├── services/             # predictive-ai.js, insertion-ai.js, ml-model.js, scheduler.js
│   ├── utils/                # weather.js (Open-Meteo, WMO codes)
│   └── scripts/              # init-db.js, seed-*.js, migrate-*.js
├── frontend/src/
│   ├── App.jsx               # Routeur (75 pages, ProtectedRoute)
│   ├── contexts/AuthContext.jsx  # Auth state + token refresh
│   ├── services/api.js       # Axios instance + interceptors
│   ├── components/Layout.jsx # Sidebar + navigation role-based
│   └── pages/                # 75 pages React
├── mobile/src/
│   ├── App.jsx               # Routeur mobile (11 pages)
│   ├── services/haptic.js    # Vibration feedback
│   └── pages/                # Parcours chauffeur-collecteur
├── ai-agent/                 # ⚠️ DÉCOMMISSIONNÉ (arbitrage A3, audit 07/2026) — remplacé par
│   └── README.md             #    le chat intégré backend/src/routes/chat.js ; code conservé, non déployé
├── deploy/
│   ├── scripts/              # deploy.sh, init-server.sh, backup.sh, health-check.sh
│   └── nginx/                # Config reverse proxy SSL
├── rapports/                 # Rapports quotidiens automatisés
├── docker-compose.yml        # Dev
├── docker-compose.prod.yml   # Production (8 services + limits mémoire)
└── docs/                     # Documentation complète
```

---

## 5. MODULES FONCTIONNELS (27 modules)

### Modules core
| # | Module | Routes API | Pages Web | Description |
|---|--------|-----------|-----------|-------------|
| 1 | Auth & Admin | auth, users, settings | Login, Users, Settings | JWT, 6 rôles (ADMIN/MANAGER/RH/COLLABORATEUR/AUTORITE/RESP_BTQ) |
| 2 | Recrutement | candidates | Candidates | Kanban 4 colonnes (Reçus/Entretien/Recrutés/Refusés), CV parsing, entretiens structurés, mise en situation |
| 3 | PCM | pcm | PersonalityMatrix, PCMTest | Test personnalité 20 questions, 6 types, scoring pondéré, export PDF A4 |
| 4 | Gestion RH | employees, teams | Employees, WorkHours, Skills, PlanningHebdo | Contrats, heures, compétences, planning hebdo 4 filières |
| 5 | Insertion | insertion, exports (insertion, fse-plus) | InsertionParcours, ActionsCIP, AuditInsertion | Entretiens historisés (5 types techniques, bilans illimités, clôture contrôlée + verrou probant), diagnostic d'accueil 12 rubriques (stepper, autosave), **9 freins** (registre unique, dont logement + judiciaire chiffré/masqué), objectifs individualisés (sous-objectifs), actions CIP rattachées aux **partenaires** (référentiel seedé), **Pass IAE** (alertes échéance), **PMSMP** + satisfaction de sortie (tables prêtes, UI en PR 2), sorties 4 catégories (dynamiques), données **FSE+** entrée/sortie + export trimestriel, alertes de fiche acquittables (journalisées), tableau de bord CIP (agenda, objectif conventionné DREETS), IA de préparation d'entretien persistée |
| 6 | Collecte | cav, vehicles, tours | Tours, CAVMap, Vehicles, LiveVehicles, FillRateMap, CollectionProposals | CAV géolocalisés, 3 modes tournée, GPS temps réel, IA prédictive |
| 7 | Tri & Production | tri, production, produits-finis | ChaineTri, Production, ProduitsFinis | 2 chaînes, batch tracking, code-barres, KPI productivité |
| 8 | Stock | stock, stock-original | Stock, AdminStockOriginal, InventaireOriginal | Mouvements entrée/sortie, inventaire physique, grand livre brut, verrouillage trimestriel Refashion |
| 9 | Expéditions | expeditions | Expeditions | Expéditions vers exutoires, bons de livraison, conteneurs |
| 10 | Facturation | billing | — | Factures HT/TVA/TTC, statuts brouillon→payée |

### Modules avancés
| # | Module | Routes API | Pages Web | Description |
|---|--------|-----------|-----------|-------------|
| 11 | Logistique Exutoires | clients/tarifs/commandes/factures-exutoires, preparations, controles-pesee, calendrier-logistique | 7 pages Exutoires* | Workflow commande 8 statuts, préparation, pesée, Gantt, calendrier |
| 12 | Reporting | reporting, historique, exports | 4 pages Reporting* | Collecte, Production, RH, Métropole |
| 13 | Refashion | refashion | Refashion | DPAV trimestriel, subventions éco-organisme REP textile |
| 14 | RGPD | rgpd | RGPD | Registre traitements, audit log, consentements, anonymisation |
| 15 | IA Prédictive | — (intégré tours) | AdminPredictive | Facteurs saisonniers, météo, événements locaux, feedback loop |
| 16 | Maintenance | — (intégré vehicles) | Vehicles | Alertes maintenance, contrôle technique, vidange, pneus, freins |
| 17 | Capteurs LoRaWAN | — (intégré cav) | — | Données capteurs IoT remplissage CAV |
| 18 | Notifications | notifications | — | SMS/email via Brevo, triggers automatiques |
| 19 | Admin DB | admin-db | AdminDB | Backup/restore, VACUUM, purge, statistiques |
| 20 | Fil d'actualités | newsfeed | NewsFeed | Articles catégorisés, épinglage |
| 21 | Référentiels | referentiels | Referentiels | Associations, exutoires, catalogue produits, conteneurs |
| 22 | Finance | finance | Finance*, FinancePL, FinanceBilan, FinanceTresorerie, FinanceControles, FinanceRentabilite, FinanceOperations | P&L analytique, bilan, trésorerie, contrôle de gestion, rentabilité matière |
| 23 | Pennylane | pennylane | Pennylane, PennylaneConfig | Synchronisation comptable **PULL-only** : Grand Livre, balances, factures clients (pas de génération) ; PUSH retiré le 03/05/2026 |
| 23bis | Contrôle facturation | factures-exutoires | ExutoiresControleFacturation | **Outil de contrôle, pas de facturation** : importe les factures clients émises sur Pennylane (`/sync/customer-invoices`), les rapproche aux commandes_exutoires (matching auto sur référence externe + libellé, fallback manuel), compare quantité facturée vs `controles_pesee.pesee_client`, alerte non-bloquante si écart > tolérance globale (`facturation_tolerance_pct` dans settings, défaut 5%), bascule automatiquement la commande en statut `cloturee` au rapprochement |
| 24 | SolidataBot | chat | — (widget flottant) | Chat IA conversationnel Claude, contexte ERP, analyse insertion/prédictif |
| 25 | Pointage | pointage | Pointage | Gestion des pointages employés |
| 26 | Boutiques | boutiques, boutique-ventes, boutique-commandes, boutique-objectifs, boutique-meteo | HubBoutiques, BoutiquesDashboard, BoutiquesVentes, BoutiquesCommandes, BoutiquesPlanning, BoutiquesObjectifs, BoutiquesImport | Performance retail 2nde main : import CSV caisse LogicS, dashboard 3 niveaux (jour/mois/année), commandes par lot/poids, objectifs mensuels, corrélation météo/CA, planning filtré |
| 27 | Vente au Kilo (VAK) | vak (CRUD + analytics/{kpis,hourly,segments,payments,comparison} + annual/{overview,hourly-heatmap,segments-trend,payment-mix} + live/current + import-csv + sumup/{status,auth-url,callback,sync,disconnect,webhook,sync-log,credentials,register-webhook}) | VakPerformance, VakAnnuel, VakSessions, VakLive, VakSumupConfig | Suivi des Ventes au Kilo mensuelles au siège (2-3 jours/mois). Caisse **SumUp** : intégration **OAuth 2.0** + sync incrémentale via `/v0.1/me/transactions/history` + **webhooks transactionnels** signés HMAC pour dashboard live temps réel (Socket.IO salle `vak:live:<id>`). Tokens chiffrés AES-256-GCM dans `settings` (helper sumup.js). Mapping description → segment (textile_vrac / chaussures / consommables). KPI distinctifs : **poids vendu (kg)**, **prix moyen €/kg**, mix paiement CB/espèces. **Écran TV** `/vak/live` (compteurs animés, jauge objectif, ticker dernières ventes). Import CSV (17 colonnes SumUp, date FR « 15 mai 2026 10:15 ») conservé en backup pour historique. Météo Open-Meteo des jours de VAK. Comparaison vs N-1 (même mois année précédente) + vs moyenne YTD. |

---

## 6. BASE DE DONNÉES (80+ tables)

### Tables principales par domaine

**Auth** : users, refresh_tokens, settings, message_templates
**Recrutement** : candidates, candidate_history, candidate_skills, skill_keywords, recruitment_interviews, mise_en_situation, recruitment_documents, recruitment_plan
**PCM** : pcm_sessions, pcm_answers, pcm_reports
**RH** : teams, employees, positions, employee_contracts, employee_availability, schedule, work_hours
**Insertion** : insertion_diagnostics, insertion_milestones, insertion_milestones_history, cip_action_plans, insertion_interview_alerts, insertion_objectifs, insertion_partenaires, insertion_pmsmp, insertion_satisfaction_sortie, insertion_alert_acks
**Collecte** : cav, vehicles, standard_routes, standard_route_cav, tours, tour_cav, tour_weights, incidents, gps_positions, tonnage_history, vehicle_checklists, cav_qr_scans
**Stock** : matieres, stock_movements, flux_sortants
**Tri** : chaines_tri, operations_tri, postes_operation, sorties_operation, categories_sortantes, types_conteneurs, produits_catalogue, produits_finis
**Exécution tri** : batch_tracking, operation_executions, operation_outputs, colisages, colisage_items, colisage_history
**Expéditions** : expeditions, associations, exutoires
**Logistique exutoires** : clients_exutoires, tarifs_exutoires, commandes_exutoires, preparations_expedition
**Facturation** : invoices, invoice_lines
**Production** : production_daily, reporting_refashion
**Refashion** : refashion_dpav, refashion_communes, refashion_subventions
**Inventaire** : inventory_batches, inventory_items
**Maintenance** : vehicle_maintenance, vehicle_maintenance_alerts
**Capteurs** : cav_sensor_readings
**IA/ML** : ml_fill_predictions, ml_model_metadata, collection_context, evenements_locaux, collection_learning_feedback
**Grille tarifaire** : grille_tarifaire
**RGPD** : rgpd_registre, rgpd_consents, rgpd_audit_log
**Objectifs** : periodic_objectives
**Finance** : financial_exercises, financial_periods, financial_entries, pennylane_config, pennylane_sync_log
**Notifications** : notification_triggers
**Historique** : historique_mensuel
**Boutiques** : boutiques, boutique_import_batches, boutique_ventes, boutique_tickets, boutique_commandes, boutique_commande_lignes, boutique_commande_historique, boutique_objectifs, boutique_meteo_quotidien
**Vente au Kilo (VAK)** : vaks, vak_import_batches, vak_tickets, vak_ventes, vak_meteo_quotidien, vak_sumup_sync_log (clés SumUp OAuth/refresh stockées chiffrées dans `settings` : `sumup.client_id`, `sumup.client_secret`, `sumup.access_token`, `sumup.refresh_token`, `sumup.token_expires_at`, `sumup.merchant_code`, `sumup.webhook_secret`, `sumup.connected_at`)

---

## 7. CONVENTIONS DE CODE

### Backend
- **Routes** : Express Router, un fichier par domaine dans `backend/src/routes/`
- **Auth** : `authenticate` middleware vérifie JWT, `authorize('ADMIN', 'RH')` vérifie le rôle
- **Base** : requêtes via `pool.query()` (pg), paramétrisées ($1, $2...)
- **Erreurs** : `res.status(code).json({ error: 'message' })`
- **Fichiers** : upload via Multer, stockés dans `/app/uploads`
- **Chiffrement** : AES-256 via crypto-js pour données sensibles (PCM)

### Frontend
- **Pages** : un fichier par page dans `frontend/src/pages/`, composant fonctionnel React
- **State** : React hooks (useState, useEffect, useCallback, useMemo, useRef)
- **Auth** : `useAuth()` hook depuis AuthContext
- **API** : `import api from '../services/api'` → `api.get()`, `api.post()`
- **Routing** : `<ProtectedRoute roles={['ADMIN', 'RH']}>`
- **Style** : Tailwind CSS, couleurs via CSS variables (--color-bg, --primary, etc.)
- **Couleur marque** : vert `#2D8C4E` (solidata-green), vert clair `#8BC540`

### Mobile
- **Navigation** : même pattern que le web (React Router)
- **Token** : stocké dans `mobile_token` / `mobile_refresh_token` (localStorage)
- **Haptic** : `vibrateSuccess()`, `vibrateError()`, `vibrateTap()` depuis services/haptic.js
- **GPS** : navigator.geolocation.watchPosition → Socket.IO emit toutes les 10s
- **Auth chauffeur (« 1 URL = 1 véhicule »)** :
  - Chaque véhicule a un `qr_token` unique (hex 32 char, espace 2¹²⁸) stocké dans `vehicles.qr_token`
  - URL d'accès = `https://m.solidata.online/v/<qr_token>` (env `MOBILE_BASE_URL`)
  - Le manager paramètre l'URL **une fois** sur le téléphone du chauffeur au dépôt (pairing D3, en personne) → chauffeur ajoute le raccourci à l'écran d'accueil → toute ouverture re-authentifie automatiquement
  - Route mobile : `/v/:token` (`VehicleLogin.jsx`) → POST `/api/auth/driver-start { vehicle_token }` → JWT + redirect `/checklist`
  - Cleanup : `history.replaceState(null, '', '/')` immédiatement après auth pour retirer le token de la barre d'adresse
  - Révocation : ADMIN appelle `POST /api/vehicles/:id/regenerate-token` → ancien raccourci renvoie 401 dès le prochain tap
  - Le `qr_token` est strippé des routes GET `/api/vehicles` et `/api/vehicles/:id` ; exposé uniquement via `GET /api/vehicles/:id/access-info` (ADMIN)
  - Nginx redact `/v/<hex32>` → `/v/REDACTED` dans les access logs (`deploy/nginx/nginx.conf`)
  - Plus de page de sélection véhicule publique (l'ancienne `Login.jsx` est devenue une landing « pas d'accès » sans énumération)

---

## 8. REGLES DE DEVELOPPEMENT

### Principes
1. **Pas de régression** — Toujours vérifier que les modules existants fonctionnent après modification
2. **Cohérence** — Suivre les patterns existants (même structure de route, même pattern de page React)
3. **Sécurité** — Toute route sensible doit utiliser `authenticate` + `authorize`. Jamais de requête SQL non paramétrisée
4. **Base de données** — Nouvelles tables via `init-db.js` (création idempotente avec `IF NOT EXISTS`). Migrations dans la section migrations de init-db.js
5. **Pas de librairie externe sauf nécessité** — Le projet est léger par design. Vérifier les dépendances existantes avant d'en ajouter
6. **Docker** — Tout changement doit fonctionner dans les conteneurs Docker. Tester le build avant de pousser
7. **Français** — L'interface est en français. Les noms de variables backend peuvent être en anglais, les labels UI sont en français

### Workflow de déploiement
```
Développeur → git push origin main → SSH serveur → bash deploy/scripts/deploy.sh update
```
Le script `deploy.sh update` fait : backup auto → git pull → docker build --no-cache → restart → prune images.

### Tests
- **Smoke tests API** : `node scripts/tests/api-smoke.js` — couvre ~40 endpoints critiques (dashboard, production, reporting, RH, finance, boutiques, référentiels) + auth + sécurité. **Hooké automatiquement dans `deploy.sh update` étape 7/7** : un 5xx sur un endpoint critique fait `error` et arrête le déploiement (recommande `git reset --hard HEAD~1 && bash deploy/scripts/deploy.sh update` pour rollback). Variables requises dans `.env` serveur : `API_USER` + `API_PASSWORD` (compte ADMIN existant).
- Backend Jest : `cd backend && npx jest` (180 tests, ~13% coverage)
- Mobile Vitest : `cd mobile && npm test` (39 tests)
- AI agent pytest : `cd ai-agent && pytest tests/` (13 tests)
- Pas de tests frontend (75 pages React sans coverage — chantier T1.5 de l'audit)

---

## 9. CONTEXTE METIER (Solidarité Textiles)

### Activité
- **Structure d'insertion** : emploie des personnes éloignées de l'emploi (CDD d'insertion, CDDI)
- **Filière textile** : collecte → tri → valorisation (réemploi, recyclage, CSR, effilochage, VAK)
- **Territoire** : Normandie, métropole de Rouen
- **Éco-organisme** : Refashion (filière REP textile, reporting DPAV trimestriel obligatoire)
- **Centre de tri** : coordonnées 49.4231°N, 1.0993°E

### Vocabulaire métier
| Terme | Définition |
|-------|-----------|
| **CAV** | Conteneur d'Apport Volontaire — point de collecte textile dans la rue |
| **PAV** | Point d'Apport Volontaire (synonyme de CAV) |
| **Exutoire** | Destinataire des produits triés (recycleur, fripier, export VAK...) |
| **CSR** | Combustible Solide de Récupération (valorisation énergétique) |
| **VAK** | Vêtements, Articles, Kilogrammes — catégorie export |
| **Crackage** | Première opération de tri (ouverture des sacs, pré-tri grossier) |
| **Refashion** | Éco-organisme de la filière REP textile (déclarations obligatoires) |
| **DPAV** | Déclaration de Points d'Apport Volontaire (reporting Refashion) |
| **PCM** | Process Communication Model — test de personnalité à 6 types |
| **CDDI** | Contrat à Durée Déterminée d'Insertion (max 24 mois) |
| **SIAE** | Structure d'Insertion par l'Activité Économique |
| **CIP** | Conseiller en Insertion Professionnelle |
| **Parcours d'insertion** | Suivi individuel avec jalons M1/M6/M12, 7 freins périphériques |
| **Filière** | Secteur d'activité interne : tri, collecte, logistique, boutique |
| **Balles** | Unité de conditionnement textile pressé (~400kg) |
| **Tare** | Poids du véhicule vide (pour calcul poids net collecté) |
| **LogicS** | Logiciel de caisse des boutiques — export CSV quotidien des ventes |
| **RESP_BTQ** | Responsable Boutique — 6ème rôle, gère les ventes et commandes de sa boutique |
| **Panier moyen** | CA TTC / nombre de tickets — indicateur clé retail |
| **IPT** | Indice Panier Ticket — articles vendus / nombre de tickets (items per transaction) |
| **Num_Ticket** | Vrai numéro de ticket LogicS (nouveau format CSV, 11 colonnes, depuis 21/04/2026) |
| **Minute key** | Clé de reconstitution ticket (YYYY-MM-DD HH:MM) — fallback pour l'ancien format CSV à 10 colonnes |

### Parties prenantes externes
- **Refashion** : éco-organisme, subventions trimestrielles
- **Métropole Rouen** : reporting territorial
- **Brevo** (ex-Sendinblue) : envoi SMS/email
- **Scaleway** : hébergement serveur
- **Let's Encrypt** : certificats SSL

---

## 10. PISTES DE DEVELOPPEMENT ET INNOVATION

### Court terme (déjà amorcé)
- [ ] Capteurs IoT LoRaWAN sur les CAV (table `cav_sensor_readings` prête)
- [ ] Maintenance prédictive véhicules (tables `vehicle_maintenance*` prêtes)
- [ ] Contrôle pesée double (table prête, UI à enrichir)
- [ ] OCR factures fournisseurs (tesseract.js déjà en dépendance)

### Moyen terme (architecture prête)
- [ ] Modèle ML de prédiction remplissage CAV (tables ML prêtes, feedback loop en place)
- [ ] Optimisation IA des tournées avec contraintes temps réel (météo, trafic, événements)
- [ ] Application mobile offline-first complète (PWA + IndexedDB)
- [ ] Dashboard temps réel avec Socket.IO (KPIs live, alertes push)
- [ ] Notifications push mobile (Service Worker)
- [ ] Intégration ERP comptable (export FEC, lien avec logiciel compta)

### Long terme (vision)
- [ ] API ouverte pour partenaires (exutoires, associations, collectivités)
- [ ] Marketplace textile inter-SIAE
- [ ] Traçabilité blockchain de la fibre textile
- [ ] Computer vision pour classification automatique au tri
- [ ] Chatbot IA d'accompagnement insertion (basé sur données PCM + parcours)
- [ ] Multi-site (plusieurs centres de tri, consolidation reporting)
- [ ] Module RSE / bilan carbone complet
- [ ] Connexion Refashion API (quand disponible, actuellement déclaratif)

### Points d'extension technique
| Point d'entrée | Fichier | Usage potentiel |
|----------------|---------|-----------------|
| Nouvelles routes API | `backend/src/routes/` + `backend/src/index.js` (app.use) | Tout nouveau module |
| Nouvelles tables | `backend/src/scripts/init-db.js` (section CREATE TABLE) | Nouvelles entités |
| Nouvelles pages | `frontend/src/pages/` + `frontend/src/App.jsx` (Route) | Nouveaux écrans |
| Navigation | `frontend/src/components/Layout.jsx` (menuSections) | Nouveau menu |
| Socket.IO | `backend/src/index.js` (io.on) | Événements temps réel |
| Jobs asynchrones | BullMQ (déjà configuré) | Tâches longues, emails, ML |
| Imports données | `backend/src/scripts/` | Seeds, migrations, imports Excel/KML |

---

## 11. FICHIERS DOCUMENTATION

| Fichier | Contenu | Public |
|---------|---------|--------|
| `CLAUDE.md` | Ce fichier — instructions IA | Développeurs + IA |
| `DOCUMENTATION_TECHNIQUE.md` | Architecture complète, BDD, API, déploiement | Technique |
| `docs/DOCUMENTATION_APPLICATIVE.md` | Comportement fonctionnel de chaque module | Fonctionnel |
| `docs/GUIDE_UTILISATEUR.md` | Guide pas-à-pas pour les utilisateurs | Utilisateurs |
| `deploy/DEPLOIEMENT.md` | Guide déploiement production | Ops |
| `RECONSTRUCTION.md` | Procédure de reconstruction complète | Disaster recovery |
| `docs/CDC_MODULE_LOGISTIQUE_EXUTOIRES.md` | Cahier des charges logistique | Spécification |
| `docs/PLAN_TESTS_COMPLET.md` | Plan de test (133 cas) | QA |
| `docs/DIAGRAMME_CHAINE_TRI.md` | Flux chaîne de tri | Métier |
| `docs/DIAGRAMME_FLUX_COMPLET.md` | Flux complet collecte→expédition | Métier |
| `docs/SCHEMA_CHAINE_TRI.md` | Schéma visuel détaillé chaîne de tri | Métier |
| `docs/SCHEMA_FLUX_MATIERES.md` | Schéma visuel flux matières entrant→sortant | Métier |
| `docs/PRESENTATION_COMPLETE_SOLIDATA.md` | Présentation complète de l'application | Direction |
| `docs/PRESENTATION_TECHNIQUE_SOLIDATA.md` | Présentation technique détaillée | Technique |
| `docs/PRESENTATION_CONSEIL_ADMINISTRATION.md` | Présentation pour le CA | Direction |
| `docs/FORMATION_CHAUFFEURS.md` | Formation chauffeurs-collecteurs (langage simplifié) | Formation |
| `docs/FORMATION_MANAGER_COLLECTE_LOGISTIQUE.md` | Formation manager collecte & logistique | Formation |
| `docs/FORMATION_MANAGER_CHAINE_TRI.md` | Formation manager chaîne de tri (pas-à-pas) | Formation |
| `docs/FORMATION_MANAGER_RH_INSERTION.md` | Formation manager RH & insertion | Formation |
| `docs/PROPOSITIONS_AMELIORATION.md` | Propositions d'amélioration UX/accessibilité | Évolution |
| `rapports/rapport-quotidien-*.md` | Rapports quotidiens automatisés (branches, sécurité, tests personas) | Ops/QA |
| `docs/LOGIQUE_TOURNEES.md` | Logique complète du module collecte/tournées (IA prédictive, OSRM, flux complet) | Technique |
| `docs/LOGIQUE_STOCK_INVENTAIRES.md` | Logique complète des deux modules de stock (moderne + original, Refashion) | Technique |
| `docs/VARIABLES_APPLICATION.md` | Toutes les variables d'environnement et de configuration | Technique |

---

## 12. HISTORIQUE DE CONSTRUCTION

| Date | Version | Changements |
|------|---------|-------------|
| 23 juillet 2026 | 2.10.0 | **Extension du module Insertion — PR 1 de 3** (plan et rapports : `rapports/insertion-2026-07-22/`, réalisation : `12-realisation-pr1.md` ; étapes 0-3 du plan de codage 05 + addendum §6bis ; 4 phases A schéma / B backend / C frontend / D vérification ; **Jest 650 → 756 verts (60 suites, +106 dont 46 contrats insertion), build Vite OK, mobile 40/40, séquence base neuve re-prouvée sur PostgreSQL 16.13 réel** (init-db → migrate-exutoires → migrate-finance → init-db ×2, schéma + requête d'acquittement vérifiés)). **Modèle « entretiens »** : `insertion_milestones` requalifiée (5 libellés figés → 5 types techniques `diagnostic_accueil`/`bilan_intermediaire`/`renouvellement`/`bilan_sortie`/`suivi_post_sortie`, titres conservés, bilans illimités « Bilan n° N », fin de l'unicité par type → index partiels accueil/sortie **par parcours** `parcours_num`), clôture contrôlée `POST /milestones/:id/close` (freins évalués ou non-évaluation assumée, `previous_review` du bilan précédent en 1 geste, prochain entretien planifié obligatoire, catégorie + check-list documents de sortie) avec **verrou probant** `locked_at` + historisation `insertion_milestones_history` (snapshots update/close/reopen, réouverture ADMIN/RH motivée, validations rendues caduques), échéancier calé contrat réel + `resyncMilestones`. **Sorties** : binaire positive/negative → **4 catégories** (`emploi_durable`/`emploi_transition`/`sortie_positive`/`autre`, legacy conservé, mapping par sortie_type), définition unifiée « dynamique » propagée (stats, audit, dashboard, métropole, FSE+) — alias `positives`/`negatives` retirés des réponses en phase D. **Diagnostic refondu** : ~45 colonnes structurées (trame officielle 12 rubriques : logement, droits, santé, budget, mobilité, linguistique CECRL, situation pro, projet pro, expression du salarié, famille, FSE+ entrée, freins) + `questionnaire_detail`/`fse_entree` JSONB, stepper à sauvegarde automatique (`statut_saisie` en_cours/complet, PUT partiel avec null explicite), **suggestions de niveau calculées côté serveur** (renvoyées par le PUT, règles documentées, jamais imposées — pré-calcul front en repli), un diagnostic par parcours. **9 freins** : registre unique `freins-registry.js` (miroir front `components/insertion/freins.js`) — 7 historiques + **logement** + **judiciaire** (art. 10 : niveau + impact organisationnel seulement, jamais suggéré) ; chiffrement applicatif AES-256 des textes santé/judiciaire (`utils/field-crypto.js`), **masquage MANAGER** (`masking.js` : jamais frein_judiciaire*/détails santé/budget), radar 9 axes à null honnête. **Lot 3** : `insertion_objectifs` (sous-objectifs 1 niveau, origine salarié/CIP, butoir), `insertion_partenaires` (16 seedés) + rattachements `cip_action_plans` (milestone facultatif, objectif, partenaire, résultat, durée), page **ActionsCIP** `/insertion/actions` (tableau transversal filtrable) + bouton « + Action » global ≤ 30 s. **Conformité IAE** : Pass IAE sur employees/candidates (recopie au link-employee avec prescripteur + éligibilité, jalons + squelette diagnostic auto), cumul CDDI 22/24 mois, jobs scheduler `checkPassIaeExpiring`/`checkRenouvellementsAPreparer`/`createPostSortieFollowups`, export **FSE+** trimestriel + export insertion Excel/CSV ; tables `insertion_pmsmp`/`insertion_satisfaction_sortie` prêtes (UI en PR 2) ; entrée registre RGPD « accompagnement socio-professionnel ». **Alertes de fiche** `GET /alertes/:employeeId` (retards, à-planifier, Pass IAE, CDDI, diagnostic, actions critiques + alertes scheduler enfin lues) avec **acquittement journalisé** `POST /alertes/:employeeId/ack` (table `insertion_alert_acks`, partagé entre CIP — fin du localStorage, phase D). **Tableau de bord CIP** : bloc « Aujourd'hui / Cette semaine » (titre réel, heure de rendez-vous, badge « Préparation IA prête » — phase D), agenda 30 j, retards groupés, objectif conventionné DREETS (settings) ; **AuditInsertion** basculée sur dynamiques/autres + cartographie 9 freins. **IA** : préparation par entretien persistée (`ia_preparation`, `?milestoneId=`), option `insertion.ia_preparation_auto` J-7. **Réglages** `GET /insertion/parametres` (REC-UX-18, défauts en code : délai diagnostic 30 j, alerte Pass IAE 7 mois, **échéance action 14 j**, **rythme bilans 2 mois**) consommés par QuickActionButton et EntretienForm. **UI** : InsertionParcours recomposée (onglets Synthèse/Diagnostic/Entretiens & bilans/Objectifs & actions/Freins/Assistant IA, composants partagés `frontend/src/components/insertion/`), onglet lecture « Parcours insertion » sur la fiche Employees, PDF exemplaire salarié (FALC)/dossier + trace de remise `remise_salarie`. Au déploiement : `deploy.sh update`, **aucun paramétrage requis** (défauts en code, réglages éditables ensuite dans settings). Résiduels PR 2 : frise FriseParcours (REC-UX-05), écran ETI (REC-UX-06), UI PMSMP/satisfaction, export 23 colonnes CDC + synthèse comité, indicateurs conventionnels AuditInsertion, AdminInsertion, purge/anonymisation nouvelles tables. |
| 13 juillet 2026 | 2.9.0 | **Vague 3 de l'audit — consolidation du socle : 8 lots de fond livrés** (8 agents d'implémentation + 1 agent debug ; 650/650 tests Jest (~169 ajoutés dont la nouvelle suite `backend/tests/contract/`), build Vite OK, mobile 40/40, séquence base neuve re-prouvée sur PostgreSQL 16 réel avec 8 vérifications de schéma ; détail : `rapports/audit-2026-07-11/05-vague3-realisation.md`). Clôt le plan d'action en 4 vagues. **Sécurité sessions** : révocation effective sans Redis (`users.token_version` dans le JWT login/refresh/driver, `authenticate` async rejette en 401 `TOKEN_REVOKED`, bump sur logout/reset/désactivation/force-logout — « forcer la déconnexion » enfin réelle), politique mdp unifiée (`validatePassword` min 10 sur les 3 points d'écriture + `must_change_password` à la création/reset), `login_failed` journalisé (email+IP, jamais le mdp) + verrouillage léger (~8 échecs/15 min → 429, jamais définitif), index + purge quotidienne `refresh_tokens`. **RGPD-IA** : `utils/pii-pseudonymize.js` (jetons « Salarié A/B » stables/requête, scrub textes libres, naissance→tranche d'âge, ré-hydratation en sortie) appliqué à `insertion-ai.js` (patronyme ne part plus à Anthropic) et `chat.js` (sanitizer sorties d'outils), entrée registre RGPD sous-traitance IA, journalisation permissions/rôles + clés API (jamais le secret). **Transactions** : PCM `/submit` atomique + `UNIQUE(session_id,question_number)`, planning `/affecter`, imports CSV boutiques/VAK (hash au succès → réimportable, FK `vak_ventes.batch_id`→SET NULL), recrutement (candidat+historique+skills, interview/mise-en-situation), tri (batches/colisages/items `FOR UPDATE`). **Observabilité** : table `job_runs` + wrapper `runInstrumented` sur 22 jobs, `GET /monitoring/jobs` + `/monitoring/realtime` (ADMIN, fraîcheur GPS/capteur/SumUp), timeouts 5 min + verrou advisory sur connexion dédiée, **dérive du scheduler corrigée** (setInterval au boot → setTimeout aligné top d'heure, gardes minute<30 retirées). **Hygiène** : schéma insertion unifié (IIFE de migration retirée d'insertion/index.js, colonnes rapatriées idempotentes dans init-db.js), code mort purgé (`cv-processor.js`, `ml.js`+montage, `Reporting.jsx` — `historique.js` conservé car il alimente le smoke test, state machines non branchées conservées comme échafaudage). **Performance** : `/cav/fill-rate` et `/cav/map` en agrégats groupés (CTE GROUP BY + LAG, ~800 sous-requêtes corrélées → 2 passes, équivalence prouvée) + cache 60 s, `generateIntelligentTour` (météo pré-chauffée 1×, prédictions parallélisées à ordre préservé, temps appris batchés ~30→1), cache lectures capteur. **Tests de contrat** (correctif systémique de T3) : `backend/tests/contract/` verrouille la forme des réponses des endpoints réparés (test→écran documenté) + bannières d'erreur sur 8 chargeurs muets restants. **Mobile** : durcissement des routes `-public` (garde JWT↔véhicule), + résiduels vague 2 (menu Seuils ADMIN/MANAGER, annulation brouillon RESP_BTQ, planning lecture filtrée RESP_BTQ). **Debug** : entrée `purgeExpiredRefreshTokens` ajoutée à la supervision (gap sécurité↔observabilité). Résiduels documentés (backlog) : double `release()` link-employee (pré-existant vague 2, bénin), `PUT /availability` et `production.js /chariots` non transactionnels, Socket.IO sans contrôle token_version, N+1 profond predictFillRate. |
| 12 juillet 2026 | 2.8.0 | **Vague 2 de l'audit — 36 chantiers livrés : ouverture aux parties prenantes** (8 agents d'implémentation + 1 agent debug ; 481/481 tests Jest (~159 ajoutés), build Vite OK, mobile 40/40, séquence base neuve prouvée sur PostgreSQL 16.13+PostGIS ; détail : `rapports/audit-2026-07-11/04-vague2-realisation.md`). **Arbitrages actés** : A2 billing retiré de l'UI (tables/services conservés, montage commenté), A3 `ai-agent/` décommissionné (chat.js = canal officiel), A4 module QHSE intégré, A5 auditeurs via rôle AUTORITE lecture seule. **Rôles** : 3 nouveaux rôles intégrés **DPO** (RGPD complet sans pleins droits), **FINANCE** (lecture seule Finance/Pennylane — garde par méthode HTTP + boutons masqués), **QHSE** (incidents+résolution, véhicules/checklists, pesées lecture, exports Refashion) ; contrainte SQL 6-rôles levée (validation applicative), landing par rôle (HomeRedirect), sections qhse/audit dans la matrice d'habilitations. **Auditeur Refashion (AUTORITE)** : module Refashion en lecture (formulaires masqués), attestation « stock verrouillé le … » sur chaque DPAV, justificatif attachable aux taux (upload ADMIN/lecture auditeur), accueil « Audit & conformité ». **Auditeur Métropole** : FillRateMap, `/metropole/kpi-insertion` (ETP/absentéisme/formation agrégés non nominatifs), délai moyen d'intervention incidents, export CSV + revue de convention PDF. **Module QHSE** (tables `qhse_events`/`qhse_habilitations`/`qhse_epi_dotations`) : registre AT/presqu'accidents avec TF1/TG sur heures réelles, habilitations à échéance (badges, synchro has_caces/permis, job d'alerte hebdo), dotations EPI, minimisation RGPD. **Tri** : page d'exécution atelier (3 étapes, écart/perte en continu) branchée sur le backend transactionnel jusqu'ici sans écran + AdminTri (chaînes/opérations/postes/catégories, famille Refashion obligatoire). **Boutiques** : cloisonnement RESP_BTQ (table `user_boutiques` + middleware de scope 403), BoutiquesPlanning routée, consolidation multi-boutiques N-1. **Direction** : KPIs de la veille par défaut (dernier jour ouvré), `GET /dashboard/alertes` consolidé (incidents/maintenance/jalons/CAV pleins/fins CDDI/stock, résilient), seuil stock paramétrable, SolidataBot +3 outils lecture (finance/insertion/ventes, double filtrage rôle). **CIP** : agenda entretiens 30j, CIP référent (`employees.cip_referent_user_id` + filtre « mes salariés »), objectif conventionné sorties dynamiques (settings), plan d'action dans le PDF. **Mobile** : canal manager→chauffeur (`driver_messages`, consigne depuis LiveVehicles, bannière FALC + « J'ai compris » offline), historique enrichi (incidents/pesées), recopie permis/CACES au link. **VAK** : remboursements gérés en API/webhook (helper `isRefundTransaction`), taux d'écoulement sur `kg_approvisionnes` saisi (aucune source stock fiable — documenté). **Nettoyage** : raccourcis exutoires bloqués avec hint + modal détail réparé, CHECK weekly_hours élargi (temps réels 24/28/30h préservés), facteurs affichés alignés sur le moteur, RECONSTRUCTION.md corrigé (ordre prouvé : init-db → migrate-exutoires → migrate-finance → init-db). **Debug** : garde RGPD du chatbot fermée aux rôles custom (resolveBaseRole — P1 de l'audit initial), QHSE ajouté aux GET vehicles/controles-pesee, doublon driver_messages supprimé. Au déploiement : créer les comptes DPO/FINANCE/QHSE/AUTORITE, affecter les RESP_BTQ à leur boutique (sinon aucune donnée visible), activer le seuil stock, saisir l'objectif sorties dynamiques. |
| 12 juillet 2026 | 2.7.0 | **Vague 1 de l'audit — 38 correctifs/chantiers livrés : réparation des chaînes de bout en bout** (9 agents d'implémentation sur lots à fichiers disjoints + 1 agent debug ; 322/322 tests Jest verts (~106 ajoutés), build Vite OK, mobile Vitest 40/40, `init-db.js` validé par double exécution sur Postgres 16 réel ; détail : `rapports/audit-2026-07-11/03-vague1-realisation.md`). **Chaîne matière→Refashion** : vues d'audit `vw_dpav_sortants`/`vw_coherence_tri_filiere` repointées de `colisages` (jamais alimentée) vers `produits_finis` (arbitrage A1, réversible, DROP+CREATE), DPAV outillée (bandeau Activité ERP + pré-remplissage `dpav-source` + badges d'écart déclaré/calculé + formulaires de saisie DPAV/communes/subventions), validation d'inventaire → écritures de régularisation, annulation de mouvement par contre-écriture auditée (`POST /stock/movements/:id/cancel`), sortie carton décrémente le stock (flux libre uniquement — les flux btq/vak sont décrémentés en aval, anti-double-compte), triple pesée réconciliée (badges d'écart), 3 voies produits_finis harmonisées (générateur unique, code-barres généré, **kiosque balance sur token de poste** `/balance/<token>`), captation par commune au prorata des CAV collectés + rattachement CAV↔commune dans AdminCAV (PATCH /communes/cav enfin câblé). **Chaîne financière** : autoMatchCommande sur référence complète avec refus si ambigu, rentabilité matière en cascade de sources réelles, `GET /finance/rapprochement-ca/:year` (CA opérationnel exutoires/boutiques/VAK/subventions vs GL 70/74, sans double compte), sync Pennylane quotidienne planifiée, tarifs exutoires réalignés (CHECK+validateur+UI), transition `→expediee` refusée sans préparation liée. **Chaîne personne** : page Prescripteurs + affectation fiche salarié + export FSE+ accessible (CohortePanel), permis B/CACES éditables + Skills.jsx refondu sur employees, durée cumulée CDDI (badge 20/23 mois) + encart fins de contrat + jalons recalés au renouvellement (`engine.resyncMilestones`), **`services/anonymization.js` unifié** (employé+candidat : santé/RQTH/naissance/titres de séjour/textes insertion/PCM/entretiens) consommé par rgpd.js et la purge planifiée, visite médicale d'embauche distincte + échéance recalculée. **Terrain** : routeur `/api/incidents` + page Incidents (cycle open→in_progress→resolved→closed, résolveur+commentaire), notes de checklist persistées + consultation sur fiche véhicule, mobile « Impossible de collecter » (5 raisons FALC, offline), photo d'incident (multipart en ligne), anti-double-affectation véhicule (4 modes), `available=true` honoré, clôture mobile idempotente. **Moteur prédictif** : normalisation par capacité (fin du kg-comme-%), `utils/fill-factors.js` source unique (appris>manuel>défaut) remplaçant 3 jeux divergents, config persistée en `settings`, boucle capteur réparée (coalesce observed_fill_rate/level), contexte LLM honnête, job quotidien de prédictions J..J+7 + `scripts/backtest-predictions.js`. **Finitions** : BoutiquesObjectifs/Ventes en HT, rayons `ca_ht`, résumé heures complet. **Debug** : régression inter-lots corrigée (double décompte stock btq/vak au scan carton), chemin « base neuve » d'init-db réparé (DO blocks avant CREATE TABLE — reconstruction from scratch refonctionne, exécuter les migrate-*.js avant le premier init-db), dernier `CLAUDE_MODEL` déprécié (vehicles.js). À communiquer au déploiement : reconfiguration unique des kiosques balance, `available=true` plus strict, panier moyen HT, FillRateMap recalibrée. |
| 11 juillet 2026 | 2.6.1 | **Vague 0 de l'audit — 26 correctifs P0 livrés** (7 agents d'implémentation sur lots à fichiers disjoints + 1 agent debug ; 216/216 tests Jest verts, build Vite OK ; détail : `rapports/audit-2026-07-11/02-vague0-realisation.md`). **Sécurité** : fin d'admin/admin123 (mdp aléatoire logué une fois + colonne `users.must_change_password` + écran bloquant de changement au 1er login (AuthContext), rattrapage auto au login en admin123), garde-fou dernier ADMIN sur `PUT /users/:id` (409), refresh refusé + purgé si compte désactivé, projection non-PII sur `GET /teams/:id` (fin de l'exposition salaire/RQTH/titre de séjour). **Chiffres réparés** : types d'heures validés strictement (400 si inconnu, sélecteur aligné sur l'enum réel normal/training/absence/sick/holiday — option « overtime » retirée du type), saisie réelle R3/R4 dans la feuille de production, boutiques en base HT unique (budget/objectifs/KPI ; table rayons relibellée CA TTC), clôture de tournée idempotente (fin du double stock au double-clic), mix paiement VAK (libellés normalisés CB/Espèces), KPI Subvention Refashion honnête (null + « en attente de saisie » au lieu d'un faux 0 €). **Écrans réparés** : FinanceOperations (contrat GET/PUT `{auto,overrides,results}` + persistance `financial_operational_data`), FinanceTresorerie (type/class sur cash_flow), FinanceControles (Actualiser→GET réel, Export CSV client), Pennylane (bouton rebranché sur `/sync/customer-invoices`), Billing (export PDF via fenêtre print A4 authentifiée), stats kanban exutoires, export CSV Refashion (axios blob authentifié), confirmation réelle de suppression de batch boutiques. **Chaînes** : `/metropole/sortie-dynamique` réécrit sur le schéma réel + définition unifiée `sortie_classification='positive'` ; le jalon « Bilan Sortie » réalisé clôt le parcours (`insertion_status='termine'` + `insertion_end_date`, idempotent, réouverture gérée). **Accès** : import paie Malibou ouvert au rôle RH (API+front), Dashboard exécutif (ADMIN/MANAGER) et Seuils d'alerte au menu, `authorize` sur `/dashboard/executive`, AdminCAV + QR batch ouverts à MANAGER (DELETE reste ADMIN, bouton masqué), Carte des CAV au 1er niveau du menu Collecte. **Divers** : require cassé de predictive-ai corrigé (`../routes/tours/predictions`), `estimated_fill_rate` fantôme retiré de l'API publique et du feedback capteur (remplacé par la vraie dernière prédiction ml_fill_predictions ou rien), smoke test : 7 chemins 404 corrigés (stock ledger, reporting production/rh/metropole, 3 routes finance). 12 points résiduels documentés pour les vagues suivantes (dont : chemin mobile `/status-public` non idempotent, clé legacy `tonnes_au_tri` dans /kpis+/rentabilite, HT/TTC résiduel BoutiquesObjectifs/Ventes, `API_USER`/`API_PASSWORD` à renseigner en prod pour le smoke authentifié). |
| 11 juillet 2026 | 2.6.0 | **Audit complet multi-agents de l'application (diagnostic, aucun code modifié)** — 43 agents en 4 volets : **fonctionnel** (14 modules, benchmark marché, score moyen 6,1/10), **technique** (14 modules, dette/sécurité/robustesse, 6,4/10), **structurel** (4 flux tracés de bout en bout — matière 5/10, personne/insertion 4,5/10, financier 5,5/10, temps réel 6,5/10 — + audit dédié des moteurs IA 5,5/10), **personas** (10 métiers, 5,5/10 ; auditeur Refashion : promesse **rompue** 2/10). 7 constats transverses majeurs : traçabilité matière rompue au milieu (colisage sans UI → vues DPAV vides), ~15 capacités backend jamais câblées au frontend, ~12 écrans faux/vides en silence (contrats front/back désalignés, catch muets, smoke test lui-même cassé), données de pilotage corrompues à la saisie (heures RH, R3/R4, HT/TTC boutiques, captation par commune), trous ciblés sécurité/RGPD (admin123, PII via /teams, anonymisation santé incomplète), moteur prédictif partiellement décoratif (kg vs %, config volatile), parties prenantes externes sans accès (AUTORITE, DPO, QHSE, Finance). Livrables dans `rapports/audit-2026-07-11/` : synthèse exécutive, **plan d'action en 4 vagues** (25 correctifs S en vague 0, recâblage des chaînes en vague 1, ouverture parties prenantes en vague 2, socle tests/transactions/monitoring en vague 3) précédé de **5 arbitrages métier** (colisage vs produits_finis, billing, ai-agent, périmètre QHSE, portail auditeurs), + 46 rapports détaillés (fonctionnel/, technique/, structurel/, personas/). |
| 10 juillet 2026 | 2.6.0 | **Liaison Recrutement ↔ Collaborateurs (profil PCM dans l'Insertion) + corrections VAK (GMT + POS=CB)**. **(A) Recrutement ↔ RH** — la base recrutement (`candidates`, saisie manuelle) et la base collaborateurs (`employees`, alimentée par le logiciel de paye Malibou) n'avaient **aucun lien automatique**, si bien que le **profil de personnalité PCM** passé au recrutement ne remontait pas dans le module Insertion (celui-ci joint `pcm_reports → pcm_sessions → candidates → employees` via `employees.candidate_id`, colonne restée nulle pour les fiches créées par la paye). **Règle métier posée** : *seul le logiciel RH crée un collaborateur* — le recrutement ne fait que **RATTACHER** une fiche à un collaborateur **existant**. **Backend** : ancien `POST /candidates/:id/convert-to-employee` (qui **créait** un employé) désactivé (410 Gone, message explicite) ; nouveaux `POST /candidates/:id/link-employee { employee_id }` (renseigne `employees.candidate_id` avec gardes d'unicité — un candidat = un collaborateur, refus si l'un des deux est déjà lié ; crée le squelette `insertion_diagnostics` pour que le PCM remonte ; trace `candidate_history`), `POST /candidates/:id/unlink-employee`, `GET /candidates/:id/employee-matches` (collaborateurs non liés classés par proximité de nom) et `GET /employees/:id/candidate-matches` (candidats non liés + drapeau `has_pcm`). `POST /employees` (création manuelle) **bloqué** (409) et `crud.js` expose `linked_employee_id`/`_name`. **Frontend** : `Candidates.jsx` remplace « Créer un employé » par « Lier à un collaborateur » (modale de recherche `LinkEmployeeModal`, badges correspondance exacte/nom proche, statut lié + « Délier ») ; `Employees.jsx` — onglet Profil PCM : « Lier une fiche de recrutement » quand non lié + « Délier », et bouton « Nouveau collaborateur » retiré (les collaborateurs viennent de l'import paye). **Prouvé sur Postgres 16 réel** : PCM invisible avant liaison → visible après (`base_type` remonte) → invisible après unlink ; `unique_violation` sur double liaison d'un candidat ; `employee-matches`/`candidate-matches`/`has_pcm` corrects. **(B) VAK — horaires en GMT** : le décalage venait d'un affichage en heure locale (Paris) alors que l'API SumUp horodate en UTC et que les graphes SQL extraient l'heure UTC. `services/sumup.js` : `parseFRDate` interprète l'export CSV en **UTC** (`Date.UTC`) et les insertions `date_ticket`/`date_vente` passent en **ISO UTC** (`.toISOString()`) → heure stockée = GMT quel que soit le fuseau du conteneur (**prouvé** : insertion identique 14:30 sous session PG Paris et UTC). Frontend (`VakLive`, `VakJournee`, `VakPerformance`, `VakAnnuel`) : heures de transaction affichées avec `timeZone: 'UTC'` + libellé « (GMT) » sur les axes/tickers. **(C) VAK — moyen de paiement POS = CB** : `payment_type` = 'POS' (carte sur terminal) était rangé en « autre ». Nouveau `normalizePaymentMethod` (POS/ECOM/VISA/Mastercard/sans-contact… → 'CB', numéraire → 'Espèces') appliqué à l'ingestion (API + CSV) ; classification centralisée (`payIsCb`/`payIsEspeces`/`payLabel`) dans `vak.js` (kpis, by-day, /payments regroupé sur libellé normalisé, annual/payment-mix) → tolérante aux données historiques 'POS'. **Prouvé sur Postgres 16** (POS+VISA+ECOM+CB comptés en CB, `EXTRACT(HOUR)` sans dérive) + **22 tests** (16 helpers via run isolé + Jest `sumup.test.js`). Frontend + backend → `deploy.sh update`. |
| 10 juillet 2026 | 2.4.7 | **Capteurs CAV — calibration niveau à deux points + détecteur de lecture figée** (symptôme signalé : « la sonde indique toujours un niveau de remplissage équivalent »). Diagnostic terrain (CAV LE HOULME, sonde Milesight EM400-MUD `24E124329F461150`) : la distance brute transmise stagnait à ~30–34 cm sur des jours (amplitude 3 cm, écart-type ~1 cm) malgré de fortes variations de température, alors qu'une lecture NFC live donnait 0,715 m — la sonde visait très probablement une **cible fixe** (obstruction dans le cône, volet d'apport, textile coincé) et non la surface réelle. L'ERP enregistrait fidèlement cette distance : ce n'était **pas** un bug ERP mais un problème d'installation + un modèle de calcul trop grossier. **(1) Calcul deux points** (`utils/milesight-em400mud.js`) : `computeFillPercent(distance, distance_empty_cm, distance_full_cm)` interpole désormais entre `distance_full_cm` (textile bon à collecter → 100 %) et `distance_empty_cm` (vide → 0 %), au lieu de supposer « plein = distance 0 ». **Rétro-compatible** : sans `distance_full_cm` (ou incohérent ≥ vide), repli exact sur l'ancien `1 − d/H` (aucune régression). Corrige le plafonnement artificiel vers ~85 % : un CAV réellement plein atteint 100 %. **(2) Colonne** `cav.sensor_distance_full_cm INTEGER` (migration idempotente `init-db.js`), câblée dans `liveobjects-processor.js` (résolution CAV + calcul), `POST /:id/sensor/provision` et `PATCH /:id/sensor-calibration` (validation 0–499 + garde `full < vide`), exposée dans `GET /sensors`, `/:id/sensor-status`, `/:id/sensor-diagnostic`. **(3) Détecteur de lecture figée** ajouté à la couche 1 du `sensor-diagnostic` : analyse l'amplitude des 20 dernières distances ; si ≤ 5 cm sur ≥ 8 lectures → `warning` + `issues` + **recommandations** (inspecter l'installation physique ; tester en désactivant « Measure Outlier Calibration » de l'app ToolBox — un filtre d'outliers trop serré peut verrouiller la mesure sur une cible proche ; renseigner la distance à plein). **(4) Frontend `SensorSection.jsx`** : champ « Distance à plein (cm) » dans les modales calibration + provisioning (garde front `full < vide`), affichage du modèle de calibration et de la **distance brute** dans les dernières lectures (pour voir la stagnation). **Prouvé** : 15 tests unitaires Jest (`milesight-em400mud.test.js` : rétro-compat mono-point = données réelles 30,9 cm→86 % / 32,9 cm→85 %, deux points, gardes de cohérence) + requête du détecteur exécutée sur **Postgres 16 réel** avec les 19 distances de la capture → `suspected_stuck = true` (amplitude 3,0 cm). Migration idempotente vérifiée. Backend + frontend → `deploy.sh update`. |
| 10 juillet 2026 | 2.5.0 | **Module Insertion — page « Audit Insertion » (synthèse de la situation de la structure)**. Nouvelle page `/insertion/audit` (sidebar « Gestion du personnel », ADMIN/RH/MANAGER) qui produit un rapport de pilotage stratégique à destination de la **direction et des CIP**. **6 sections** : (1) indicateurs clés (personnes en parcours, taux de réalisation des jalons échus, plans d'action en cours, taux de sorties dynamiques) ; (2) **taux de réalisation des entretiens/bilans par échéance** (barres par type de jalon = réalisés parmi les échus) ; (3) **diagramme en toile d'araignée consolidé des 7 freins** (SVG, moyenne cohorte /5, frein dominant) ; (4) **plans d'action en cours** (répartition statut/catégorie/priorité) ; (5) **sorties + statistiques** (total, dynamiques/autres, taux, par type) ; (6) **rapport IA de situation globale**. **Backend** : helper mutualisé `gatherAuditKpis(year)` (résilient — chaque requête en échec dégrade, ne casse pas l'audit) exposé par `GET /api/insertion/audit` (ADMIN/RH/MANAGER, agrégats **non nominatifs**) ; `GET /api/insertion/audit/ia` (**ADMIN/RH**) croise ces indicateurs chiffrés avec les **verbatims anonymisés des CIP/agents** (observations de diagnostic, bilans de jalons réalisés, notes d'actions — sans aucun nom) et appelle `insertion-ai.auditGlobalReport()` : prompt « rapport de situation globale pour la direction » → JSON `{ synthese_direction, situation_globale, profil_public, points_forts[], points_vigilance[], recommandations_structure[{action,objectif,echeance_suggeree}], conclusion }` (max_tokens 3000, `handleIaError`, timeout front 120 s). Les 2 routes audit sont placées **avant** `/:employeeId` (sinon capturées). **Frontend** (`AuditInsertion.jsx`) : radar SVG autonome, cartes KPI, barres de réalisation, sélecteur d'année, bouton « Générer le rapport IA » (ADMIN/RH), **export PDF** (fenêtre d'impression A4 dédiée, même mécanisme que les fiches parcours/PCM — inclut le rapport IA s'il a été généré). **Agrégats SQL prouvés sur Postgres 16** (en parcours=3 hors terminé/none, taux jalons par échéance, moyennes freins 3,67/3,00/2,67, actions en cours=3, sorties 1 dynamique + 1 autre) ; **build Vite complet OK** (chunk AuditInsertion lazy-loadé). **Export PDF refondu à la charte** : reprend les **visuels** (radar SVG des 7 freins reconstruit dans le HTML d'impression, cartes KPI, barres colorées de réalisation vert/ambre/rouge selon le taux, encarts sorties, mini-tables actions) et l'**identité graphique** existante (en-tête teal `#0D9488` « SOLIDATA — Audit Insertion » comme les fiches PCM/parcours, titres de section soulignés, cartes `.card`/`.hl`, rapport IA structuré) — **vérifié par rendu Chromium réel** (Playwright headless : PNG + PDF A4 générés et inspectés). Frontend + backend → `deploy.sh update`. |
| 9 juillet 2026 | 2.4.6 | **Module Insertion — extraction complète des données (Excel multi-feuilles)**. Nouvel endpoint `GET /api/exports/insertion` (dans `routes/exports.js`, **ADMIN/RH** — le router autorise ADMIN/MANAGER/RH, resserré au niveau de la route car données personnelles sensibles : freins santé/social) qui génère un classeur `.xlsx` (exceljs, même pattern que les autres exports) avec **5 feuilles** : `Informations` (métadonnées + avertissement RGPD + compteurs), `Salariés` (vue synthèse curée : matricule, identité, poste, filière, statut, dates parcours/contrat, prescripteur+type, visite médicale, 7 freins du diagnostic, nb jalons / réalisés), `Diagnostics CIP`, `Jalons`, `Plans d'action` (ces 3 dernières en **colonnes dynamiques** = *toutes* les colonnes présentes, matricule/nom/prénom en tête, résilient au schéma variable). Périmètre = tout salarié avec `insertion_status <> 'none'` **ou** un diagnostic **ou** un jalon (les non-concernés sont exclus). **Résilience** : chaque requête passe par un helper `soft()` (une requête qui échoue sur une colonne absente → feuille vide + log, l'export ne casse pas) + repli minimal sur la feuille Salariés ; `fmtCell` sérialise dates (ISO), tableaux et JSONB (`ai_recommendations`) en texte. **Frontend** (`InsertionParcours.jsx › CohortePanel`) : bouton **« Exporter (Excel) »** dans l'en-tête du tableau de bord CIP (visible ADMIN/RH via `useAuth` + `base_role`), téléchargement via axios `responseType: blob` (transmet le Bearer) → `insertion_complet_<date>.xlsx`, timeout 120 s, bandeau d'erreur (lecture du blob JSON en repli). **Prouvé bout-en-bout sur Postgres 16 + exceljs** (5 feuilles, salarié `none` exclu, en-têtes ordonnés, JSONB sérialisé, fichier .xlsx relu). **Option CSV** : le même endpoint accepte `?format=csv&dataset=salaries|diagnostics|jalons|actions` et renvoie un CSV point-virgule + BOM (ouverture directe Excel FR, champs échappés) — CSV étant mono-table, un jeu de données par fichier ; helper `toCsv()` partagé, sérialisation prouvée (échappement `;`/`"`/retour-ligne, dates ISO, JSONB, dataset vide). **Frontend** : le bouton devient un **sélecteur de format** (menu déroulant « Excel — tout » / « CSV — Salariés / Diagnostics / Jalons / Plans d'action ») + bouton « Exporter », nom de fichier adapté (`insertion_<dataset>_<date>.csv`). Frontend + backend → `deploy.sh update`. |
| 7 juillet 2026 | 2.4.5 | **Fix fil d'actualité — « sur clic je n'accède pas au contenu de l'article »**. Deux surfaces ne permettaient pas d'ouvrir un article. (1) **Widget d'accueil** (`Dashboard.jsx › NewsWidget`) : les items de la liste n'avaient **aucun `onClick`** — cliquer un titre ne faisait rien. Correctif : chaque item devient un `<button>` cliquable (hover, focus a11y) qui navigue vers `/news?article=<id>`. (2) **Page `/news`** (`NewsFeed.jsx`) : le bouton « Lire l'article complet / Voir la source » ne s'affichait que si `hasMore = article.content || article.source_url`, or les articles de **veille automatique n'ont que `summary`** (ni `content` ni `source_url`) → aucun élément cliquable, et le titre n'était pas cliquable. Correctifs : le **titre devient cliquable** (déplie l'article quand il a du contenu/une source) ; **deep-link `?article=<id>`** (via `useSearchParams`) → à l'arrivée depuis le widget, l'article est déplié si possible, la page **défile jusqu'à lui** (`scrollIntoView`) et il est **surligné** brièvement (`ring-2 ring-primary`, 2,8 s) pour le repérer. Le `summary` étant déjà affiché en toutes lettres sur `/news`, l'utilisateur « accède » bien au contenu de la brève. NB : les brèves de veille sont **courtes par conception** (titre + 2-3 phrases) ; pour un article complet avec corps + lien source, utiliser « Publier » (champs `content` + `source_url` déjà gérés) ou brancher un flux RSS réel (évolution possible). Frontend uniquement → `deploy.sh update` (rebuild image frontend). |
| 7 juillet 2026 | 2.4.3 | **Hotfix « ⚠ Erreur analyse IA » (module Insertion) — diagnosticabilité**. Sur `/insertion`, « Analyse IA approfondie (Claude) » → « Analyser le profil » / « Préparer entretien » (et le bilan de cohorte) renvoyaient un « ⚠ Erreur analyse IA » **opaque** : la clé `ANTHROPIC_API_KEY` étant bien présente (le guard 503 ne se déclenchait pas), l'échec venait de l'appel `anthropic.messages.create` lui-même (le plus souvent : modèle `CLAUDE_MODEL` non autorisé pour la clé → 404, clé révoquée → 401, ou quota → 429), mais le backend écrasait la vraie cause. Correctif **backend** (`routes/insertion/routes.js`) : helper mutualisé `handleIaError(res, err, where)` sur les 3 routes IA (`/ia/profil`, `/ia/entretien`, `/ia/cohorte`) qui log le `status` HTTP du SDK Anthropic + `message`, et renvoie un `hint` ciblé selon le code (404/modèle → « le modèle « … » n'est pas disponible pour cette clé, définissez `CLAUDE_MODEL` sur un modèle autorisé puis redémarrez » ; 401 → clé invalide/révoquée ; 429 → quota atteint). Ces endpoints étant **ADMIN/RH uniquement**, exposer l'indice technique est acceptable. Correctif **frontend** (`InsertionParcours.jsx`) : les 3 handlers d'erreur IA (les 2 boutons + `runIaCohorte` du panneau cohorte) concatènent désormais le `hint` au message affiché (` — <hint> `). NB : le modèle par défaut reste `claude-sonnet-5` (aligné compose + code) ; ce correctif ne change pas le modèle, il **rend l'erreur lisible** pour que l'admin corrige `CLAUDE_MODEL`/la clé côté `.env` serveur. **Sonde de diagnostic isolée** (suite du debug) : nouvel endpoint `GET /api/insertion/ia/diagnostic` (ADMIN/RH) qui teste **purement** l'appel Anthropic (clé + modèle + connectivité réseau) hors de toute logique de collecte de données salarié — un `messages.create` minimal (`max_tokens:8`, « ping ») renvoyant `{ configured, ok, model, key_length, latency_ms, status, type, message }` bruts ; bouton « Tester la connexion IA » ajouté au panneau IA de `InsertionParcours.jsx` (bandeau vert si OK / ambre avec status+type+message si échec). Permet de distinguer instantanément un problème de **réseau** (le conteneur backend ne joint pas `api.anthropic.com` → `APIConnectionError`, sans status HTTP) d'un problème de **clé/modèle** (401/404). **Cause racine identifiée par la sonde** : le diagnostic est ressorti **✓ vert** (clé longueur 108, modèle `claude-sonnet-5`, réponse « Pong! » en ~1,9 s) → l'appel Anthropic marche, l'échec de « Analyser le profil » venait donc de `services/insertion-ai.js › getEmployeeInsertionData` : ses **6 requêtes SQL** tournaient dans un `Promise.all` où **une seule** requête auxiliaire échouant sur une colonne absente d'une base ancienne (`employees.candidate_id`, `candidates.practical_test_result`/`cv_raw_text`, `pcm_reports.risk_alert`…) **rejetait toute l'analyse**. Correctif **résilience** : helper `soft(text, params, label)` qui dégrade chaque requête **secondaire** en `{ rows: [] }` + log serveur (`[INSERTION-AI] requête « … » ignorée (code) : message`) ; seule la requête « salarié » reste essentielle, avec **repli minimal `SELECT *`** si une colonne jointe manque. Corrige simultanément « Préparer entretien » (même fonction partagée `getEmployeeInsertionData`) et durcit les 2 requêtes secondaires (`retards`, `actionsRetard`) de `bilanCohorte`. Prouvé en isolation (requête candidat forcée en `42703` : avant → analyse échoue ; après → réussit, candidat omis, salarié conservé). Les bandeaux d'erreur IA affichent en repli le `detail` brut quand aucun `hint` ciblé ne s'applique. **Cause racine FINALE (timeout)** : après déploiement, la sonde restait **✓ verte** et le bilan de cohorte réussissait (~20 s), mais « Analyser le profil » affichait « Erreur analyse IA » **sans produire aucune requête backend** (logs vides). Diagnostic : le `timeout` global d'axios (`services/api.js`) est de **30 s** ; l'analyse de profil (prompt plus riche + `max_tokens 2500`) **dépasse 30 s** → axios abort `ECONNABORTED` **sans `response`** → bandeau générique, et le backend loguait la requête seulement à sa complétion (après le pull des logs). Correctif : **timeout dédié de 120 s** (`IA_TIMEOUT`) passé en option sur les 3 appels IA (`api.get(url, { timeout: IA_TIMEOUT })`) — nginx autorisait déjà 300 s sur `/api`. Helper front partagé `formatIaError(err, fallback)` : message de **timeout explicite** (« L'analyse IA a dépassé le délai… réessayez »), gestion 503, sinon `error` + `hint`/`detail`. Nécessite `deploy.sh update` (rebuild images backend + frontend). |
| 7 juillet 2026 | 2.4.4 | **Fix fil d'actualité — auto-alimentation figée (« ne s'actualise plus automatiquement »)**. Le job scheduler `autoFeedNews()` (veille sectorielle, 2 articles/j max, exécuté à 7h/12h/18h via `runAllJobs`) n'insérait plus rien depuis plusieurs jours. Cause racine : la **déduplication par titre était définitive** — `SELECT id FROM news_articles WHERE title = $1` bloquait tout titre **ayant déjà existé un jour**. Le catalogue étant **fini** (12 thèmes canoniques), une fois les 12 titres tous publiés (≈6 j à 2/j), **chaque tirage aléatoire tombait sur un titre existant → `continue` → plus aucune publication** → le fil se figeait. Correctif : dédup **fenêtrée à 21 jours** (`WHERE title = $1 AND created_at > NOW() - INTERVAL '21 days'`) → un thème redevient éligible après 21 j, le fil **recycle** son catalogue et ne s'assèche jamais, sans republier le même thème à moins de 3 semaines d'intervalle. Catalogue **élargi de 12 → 22 thèmes** (textile : tri optique, recyclage chimique, CSR, éco-conception, affichage environnemental ; ESS/insertion : clauses sociales, numérique inclusif, santé/prévention QHSE, France Travail, sortie dynamique DREETS) pour plus de variété et une cadence proche de 2/j. Prouvé sur Postgres 16 réel : titre vieux de 40 j → **éligible/republié**, titre vieux de 2 j → **bloqué**. Nécessite `deploy.sh update` (rebuild image backend). |
| 7 juillet 2026 | 2.4.2 | **Hotfix sauvegarde Admin DB (suite) — permission sur le volume backups**. Après le correctif 2.3.3 (`pg_dump` présent), la sauvegarde échouait désormais en `pg_dump: error: could not open output file "/app/backups/…": Permission denied`. Cause : le volume nommé `backups` monté sur `/app/backups` appartenait à **root** (Docker crée le point de montage en root quand le dossier n'existe pas dans l'image), alors que le conteneur tourne en `appuser` (uid 1001) — contrairement à `uploads` dont le dossier existe dans l'image (créé par `appuser`). Correctif symétrique à `uploads` : (1) `Dockerfile` crée `/app/backups` **en tant qu'appuser** (`mkdir -p /app/uploads /app/backups`) → un volume neuf hérite de cet owner ; (2) le volume est **renommé** `solidata-backups` → `solidata-backups-v2` pour forcer une recréation propre (l'ancien, vide et root, est abandonné) — pas de `docker volume rm` manuel requis. L'erreur remonte désormais le stderr `pg_dump` (grâce au `hint` de 2.3.3), ce qui a permis le diagnostic. Nécessite `deploy.sh update` (rebuild image + recreate). |
| 7 juillet 2026 | 2.4.1 | **Rôles personnalisés (duplication d'un rôle)**. Sur `/admin/permissions`, possibilité de **créer un rôle en dupliquant un rôle existant** (ex. dupliquer « RH » → « Opérations », « Logistique »). Le rôle personnalisé **hérite des accès de son rôle de base** (le rôle source, jamais ADMIN → pas d'escalade) puis se restreint via la matrice de modules. **Backend** : table `custom_roles(role_key, label, base_role)` ; **migration `users.role`** — lève la contrainte `CHECK` (limitée aux 6 rôles) et élargit en `VARCHAR(50)` (DO block scannant `pg_constraint`), la validation du rôle passe en applicatif (`isValidRole` = intégré ∪ custom) ; `middleware/auth.authorize` **résout custom→base** (cache rafraîchi 60 s + à chaud via `refreshCustomRoles`, additif : les rôles intégrés sont inchangés) ; `resolveBaseRole` exporté ; `GET /auth/me` renvoie `base_role`. Nouvelles routes `permissions.js` : `GET /roles`, `POST /roles` (duplique : copie les habilitations module du rôle source), `DELETE /roles/:key` (réaffecte les utilisateurs au rôle de base puis nettoie). `role_module_access.role` élargi en `VARCHAR(50)`. **Front** : `Layout` + `ProtectedRoute` filtrent sur `base_role` (un rôle custom voit ce que voit son modèle) ; `AdminPermissions` = panneau « Créer un rôle (duplication) » + colonnes custom supprimables dans la matrice ; `Users` peuple ses listes déroulantes depuis `/permissions/roles` (intégrés + custom). **Vérifié bout-en-bout sur Postgres 16** : avant migration l'assignation d'un rôle custom échoue (CHECK 23514) → migration (VARCHAR 50, 0 CHECK) → duplication RH→Opérations copie les habilitations → assignation OK → `my-modules` custom → résolution vers RH → suppression réaffecte à RH. |
| 7 juillet 2026 | 2.4.0 | **Gestion des utilisateurs + habilitations par module**. (1) **Édition utilisateur** dans `/users` : le formulaire manquait — ajout d'une modale « Modifier » (rôle/habilitation, email, nom, actif via `PUT /users/:id`) et d'une **réinitialisation de mot de passe** (`PUT /users/:id/reset-password`, invalide les sessions). Les endpoints backend existaient déjà, seul le front les câble. (2) **Habilitations par module** (nouvelle page `/admin/permissions`) : contrôle, **par rôle**, la visibilité des modules (sections de 1er niveau de la sidebar) — ex. masquer « Tri » aux Managers RH. Nouvelle table `role_module_access(role, module_key, allowed)` (**DENY-overlay** : absence de ligne = autorisé ; on ne peut que **restreindre**, jamais élargir, donc pas d'escalade). Nouveau `routes/permissions.js` : `GET /my-modules` (tout authentifié, **fail-open**), `GET /catalog`, `GET /matrix`, `PUT /matrix` (ADMIN). L'**ADMIN n'est jamais restreint** (anti-lockout). Front : `AuthContext` charge les modules refusés + expose `canAccessModule(key)` ; `Layout` filtre les sections de 1er niveau du `NAV_TREE` (par `id`) en plus du filtre rôle ; page `AdminPermissions` = matrice cases à cocher rôles × 9 modules. Vérifié sur Postgres réel (table + upsert idempotent + my-modules). **Périmètre** : masquage de la sidebar (le besoin exprimé « n'a pas besoin de voir Tri ») ; le blocage dur au niveau route reste sur le filtre rôle existant (`ProtectedRoute`). |
| 7 juillet 2026 | 2.3.3 | **Hotfix sauvegarde Admin DB — `pg_dump` absent du conteneur API**. « Erreur lors de la sauvegarde » sur `/admin-db` (et la restauration l'aurait été aussi). Cause racine : l'image backend (`node:20-alpine`) n'embarquait **pas** le client PostgreSQL, donc `execFileSync('pg_dump'…)` (et `psql` pour la restauration) échouait en `ENOENT`. Correctif : `Dockerfile` installe `postgresql15-client` (aligné sur le serveur `postgis/postgis:15-3.4`, fallback méta-paquet). Smoke test réel : la commande exacte du endpoint (`pg_dump --no-owner --no-acl -f …` + env `PG*`) produit bien un fichier. **Persistance** : les sauvegardes écrites dans `/app/backups` étaient éphémères (aucun volume) → nouveau volume nommé `backups:/app/backups` (comme `uploads`), elles survivent aux redéploiements. **Diagnosticabilité** : backup/restore renvoient un `hint` (ENOENT → « client PostgreSQL absent », sinon stderr tronqué) au lieu d'un message opaque, affiché côté UI. **Bonus** : défaut `CLAUDE_MODEL` du compose aligné sur le code (`claude-sonnet-4-20250514` déprécié → `claude-sonnet-5`). Nécessite un rebuild de l'image API (`deploy.sh update` fait `docker build --no-cache`). |
| 7 juillet 2026 | 2.3.2 | **Hotfix enregistrement diagnostic CIP — colonnes manquantes**. « Erreur serveur » à l'enregistrement d'un diagnostic (ex. Allan Bainey). Cause racine reproduite sur Postgres 16 local : `PUT /insertion/diagnostic/:id` écrit des colonnes qui **n'existaient pas** sur les bases anciennes (`column "…" of relation "insertion_diagnostics" does not exist`, SQLSTATE 42703) — les deux définitions `CREATE TABLE insertion_diagnostics` (init-db.js / insertion index.js) avaient divergé et la liste `addCol` de migration était **incomplète** (elle ajoutait les `frein_*_causes` + certains `explorama_*` mais pas `cip_questions`, `explorama_interets`, `pref_*`, `frein_*_detail`, `created_by/updated_by`…). Correctif : migration idempotente **exhaustive** dans `insertion/index.js` garantissant l'existence de **toutes** les colonnes écrites par le PUT (26 TEXT + 7 freins INTEGER + created_by/updated_by/updated_at). La requête elle-même était valide (prouvé : OK sur schéma complet, y compris frein `null`). Erreur rendue diagnosticable : le PUT renvoie désormais le `code` SQLSTATE + un `hint` (« base non à jour ») au lieu d'un « Erreur serveur » opaque. Vérifié bout-en-bout : base ancienne → PUT échoue (42703) → migration → PUT réussit + réenregistrement idempotent. |
| 7 juillet 2026 | 2.3.1 | **Hotfix import collaborateurs — robustesse transactionnelle + colonnes texte**. Deux bugs remontés en prod à l'import (« value too long for type character varying(30) » suivi de « current transaction is aborted » en cascade sur tous les collaborateurs suivants). (1) **Cause racine cascade** : tout l'import tournait dans **une seule transaction** ; une ligne fautive avortait la transaction entière → aucun collaborateur importé. Correctif : **SAVEPOINT par collaborateur** dans `upsertCollaborators` (`SAVEPOINT` → `RELEASE` au succès, `ROLLBACK TO SAVEPOINT` à l'échec) → une ligne fautive est isolée et signalée, les autres passent. Prouvé par test (`test_savepoint.js` : ligne fautive isolée, 4/5 importés, pas de cascade). (2) **Cause overflow** : la colonne `residence_permit_renewal` (et autres champs texte issus de l'export à longueur non maîtrisée) était en `VARCHAR(30)`. Migration idempotente `init-db.js` (DO block + `information_schema`) passant 20 colonnes d'import (`address`, `city`, `civility`, `disability_status`, `residence_permit_*`, `medical_visit_frequency`, `work_time_type`, `gross_salary`, `siret`, `establishment`, `qualification`…) en `TEXT`. Idempotence de l'import préservée (test réel : 56 lus, réimport 0 créé / 56 màj). |
| 7 juillet 2026 | 2.3.0 | **Module Insertion — implémentation de la feuille de route du diagnostic CIP (quick wins + structurants)**. Suite au diagnostic 2.2.0, l'intégralité du plan d'action est livrée. **Backend** : (1) **Jalons calés sur le contrat réel** — helper partagé `engine.computeMilestoneSchedule(start,end)` (Diagnostic M+1, M+3/M+6/M+10 seulement s'ils tombent avant la fin, Sortie = fin−15 j) ; fini les jalons fantômes après la fin des contrats courts (vérifié : contrat 6 mois → 3 jalons, 12 mois → 5). Réutilisé par les routes ET le scheduler (`checkInsertionMilestones` aligné). (2) **Auto-initialisation** — `POST /milestones/:id/initialize` démarre le parcours (statut + date) et pose les jalons ; **auto-init paresseuse** à la 1re ouverture d'une fiche `en_parcours` sans jalon (fin du clic manuel obligatoire). (3) **Auto-statut à l'import** — un poste « … Cddi » crée le salarié directement `en_parcours` (start = début de contrat), sans réactiver un parcours clôturé au réimport. (4) **Endpoint pilotage** `GET /insertion/cohorte/stats` (algorithmique, sans dépendance IA) : jalons en retard / à venir 7 j, salariés à risque (fin contrat < 60 j), freins moyens + dominant, **taux de sorties dynamiques** annuel (KPI DREETS/ASP). (5) **Prescripteur** joint dans `GET /:employeeId` (nom + type), exposé pour le reporting FSE+. (6) **Freins non faussés** — le PUT diagnostic n'écrit plus « 1 » par défaut (→ `null` = non évalué, exclu des moyennes). (7) **Modèle IA** `insertion-ai.js` : défaut `claude-sonnet-4` déprécié → `claude-sonnet-5` (surchargeable via `CLAUDE_MODEL`). NB : les jobs scheduler `checkInsertionMilestones` + `checkInsertionInterviewAlerts` (alertes J-7/J-1/retard + notif Brevo) existaient déjà et alimentent `insertion_interview_alerts`. **Frontend `InsertionParcours.jsx`** : (a) **Tableau de bord CIP** (`CohortePanel`) — vue pilotage par défaut : cartes KPI (en parcours, retards, à-venir 7 j, taux sorties dynamiques), listes cliquables (retards / à planifier / fins de contrat) qui ouvrent la fiche, freins moyens de cohorte, sorties par type. (b) **Export PDF** fiche parcours + bilan de jalon (impression A4 navigateur, même mécanisme que le PCM, aucune lib ajoutée). (c) **Freins « à évaluer »** (plus de faux « 1/5 »), **pré-remplissage** du bilan depuis le dernier jalon réalisé (freins + objectifs). (d) **Erreurs remontées** — fin des `catch {}` muets et des `alert()` (bandeaux d'erreur ; l'échec IA ne s'affiche plus comme une « synthèse »). (e) **Assistant IA** clarifié (onglets « Synthèse & métiers » / « Assistant IA », états de chargement séparés) + affichage des sorties IA jusque-là masquées (signaux d'alerte, freins prioritaires, vigilances PCM, points de vigilance). (f) **Prescripteur** affiché dans l'en-tête. |
| 7 juillet 2026 | 2.2.0 | **Import collaborateurs idempotent + fiche salarié complète + diagnostic CIP insertion**. (1) **Fix doublons** : l'import RH ne fait plus « supprimer tout puis réinsérer » (qui laissait un fantôme inactif par salarié à chaque passage, d'où les paires Actif/Inactif observées). Nouveau service `services/collaborator-import.js` : upsert **idempotent** apparié par `malibou_id` (Matricule) puis nom+prénom normalisé (casse/accents), fusion **non destructive** (COALESCE — un champ vide n'écrase jamais une valeur saisie), `is_active` piloté par la colonne « Contrat actif » de l'export. Réimporter le même fichier crée 0 / met à jour N (prouvé sur l'export réel : 56 lus, import#2 = 0 créé / 56 màj). (2) **Import direct du classeur Malibou (.xlsx)** : `POST /api/employees/import/xlsx` (multer mémoire + exceljs) lit les feuilles « Informations salariés » + « Contrats » (dernier contrat par matricule, apparié sur date d'avenant/début) ; voie CSV `POST /import/csv` conservée mais convertie en upsert. **~20 nouvelles colonnes `employees`** : address/city/postal_code/country, birth_city/birth_country/birth_department, civility, disability_status (RQTH), residence_permit_type/number/renewal, medical_visit_frequency, seniority_date (ancienneté), manager_malibou_id/manager_name + `manager_id` FK auto-résolu en 2e passe, work_time_type, gross_salary, siret, establishment. **RGPD (minimisation)** : NIR / IBAN / BIC présents dans l'export **volontairement NON importés** (données de paie, aucun usage ERP, réduction surface d'exposition). (3) **Nettoyage des doublons hérités** : `POST /import/dedupe-ghosts` supprime les fiches fantômes inactives sans matricule en doublon d'un actif homonyme, en **préservant** celles rattachées à un historique métier (delete isolé par fiche + try/catch). (4) **Édition complète d'un collaborateur** : `PUT /employees/:id` (liste `allowed` étendue) + formulaire `Employees.jsx` avec bloc repliable « Informations détaillées » (identité, coordonnées, naissance, IAE). (5) **Diagnostic CIP** du module insertion (`rapports/2026-07-07-diagnostic-cip-module-insertion.md`) : frictions relevées (parcours non auto-initialisé, jalons M+1/3/6/10/12 fixes non calés sur la durée réelle du contrat, défaut trompeur « 1/5 » sur freins non évalués, `bilanCohorte` codée mais jamais branchée, aucun taux de sorties dynamiques DREETS agrégé, pas d'export PDF, modèle IA `claude-sonnet-4` déprécié) + feuille de route priorisée (quick wins / structurants / vision). Page d'import refondue (choix .xlsx/CSV, compteurs créés/màj/erreurs, bouton maintenance). |
| 8 mars 2026 | 1.0.0 | Version initiale — 15 modules, 37 tables, 22 routes, 28 pages web, 11 pages mobile |
| 10-15 mars 2026 | 1.1.0 | Module logistique exutoires complet (7 pages, 6 routes API, 4 tables) |
| 16-18 mars 2026 | 1.1.x | Corrections UX, Tailwind mobile, dépannage SSL, tests déploiement |
| 19 mars 2026 | 1.2.0 | Fix moteur PCM (immeuble base fondation, Q7→stress, exports PDF A4), documentation exhaustive |
| 24 mars 2026 | 1.2.1 | Documentation complète : schémas visuels, 3 présentations, 4 supports de formation par profil, propositions d'amélioration |
| 25-29 mars 2026 | 1.3.0 | Module Finance (7 pages, Pennylane sync), SolidataBot IA (chat Claude), maintenance véhicules avancée, IA prédictive collecte + insertion, dashboard amélioré, pointage, auth mobile simplifiée, 29 commits, +12 497 lignes |
| 30 mars 2026 | 1.3.1 | AdminCAV enrichi (fiche détaillée, photo, carte GPS, planches QR), QR codes CAV (génération, planches PDF, import 209 CAV), plan entretien constructeur IA, mobile sans auth (flux véhicule→tournée simplifié), 19 commits. Audit sécurité : 3 injections SQL identifiées, 31 bugs recensés (7 critiques). 7 branches obsolètes identifiées pour nettoyage |
| 31 mars 2026 | 1.3.2 | Algorithme tournée v2 (OSRM + pause déjeuner + GPS), système suivi logs admin, script QR codes manquants, fix driver_id→driver_employee_id, 5 commits. Audit complet : 19 vulnérabilités sécurité (5 critiques), 25 bugs personas (4 critiques). Note globale 6.7/10 |
| 2 avril 2026 | 1.3.2 | Revue de projet : 7 branches obsolètes (toutes intégrées dans main), audit sécurité (7.5/10), audit npm (7 vuln dont 4 HAUTE), tests 4 personas (18 bugs dont 3 BLOQUANTS : mismatch Socket.IO GPS, Production KPI noms champs, ProduitsFinis noms champs). Note globale 6.8/10. 184 commits |
| 3 avril 2026 | 1.3.2 | Audit quotidien : repo propre (1 branche, 7 obsolètes nettoyées), audit sécurité (18 vuln dont 3 critiques : injection shell admin-db, mots de passe par défaut), tests 4 personas élargi (43 bugs dont 6 BLOQUANTS : Socket.IO GPS, ProduitsFinis champs, Production KPI, TourSummary structure, Expeditions champs). 4 bugs bloquants récurrents non corrigés. Note globale 6.5/10. 180 commits |
| 4 avril 2026 | 1.3.2 | Audit le plus approfondi (4 personas complètes). 7 branches obsolètes. Sécurité **5.5/10** : 2 critiques, 8 hautes, 7 vuln npm. **76 bugs dont 18 BLOQUANTS** — 6 modules cassés : Dashboard (SQL crash), WorkHours (100% non-fonctionnel), Mobile Chauffeur (CHECK constraint, données perdues), Expéditions (mismatch champs complet), Commandes Exutoires (workflow bloqué statut `chargée`), ProduitsFinis (création+affichage). Note globale **4.7/10**. Sprint correctif urgent recommandé (6-8h). 179 commits |
| 6 avril 2026 | 1.3.2 | Audit quotidien : 11 commits orphelins réintégrés (design system lot 1, fix vue mv_cav_stats). Repo propre (1 branche). Sécurité **4.5/10** (6 CRITIQUES : injection SQL insertion/index.js, injection shell admin-db, auth manquante PCM). 10 vuln npm (8 HIGH). **39 bugs dont 7 BLOQUANTS** — 7 modules cassés : Dashboard (SQL crash), WorkHours (endpoints incompatibles), ProduitsFinis (mismatch champs), GPS temps réel (Socket.IO), LiveVehicles, Expéditions (date mismatch), FillRateMap (structure réponse). 4 bugs bloquants récurrents depuis 02/04. Note globale **4.8/10**. 155 commits |
| 7 avril 2026 | 1.3.2 | Audit quotidien : 15 commits orphelins réintégrés sur main. Repo propre (1 branche). Sécurité **4.5/10** (2 CRITIQUES : injection SQL insertion/index.js, injection shell admin-db + 6 HAUTES dont PCM sans auth). 11 vuln npm (9 HIGH). **36 bugs dont 12 BLOQUANTS** — 10 modules cassés : Mobile GPS (Socket.IO mismatch+auth), Mobile retour (CHECK constraint), WorkHours (triple incompatibilité), Expéditions (champs POST), Commandes Exutoires (status `chargée`), LiveVehicles (Socket.IO), Dashboard KPIs (colonne erronée), ProduitsFinis (JOIN manquant), Reporting (paramètre API), ChaineTri (nb_postes). **8 bugs récurrents ≥3 jours non corrigés**. Note globale **4.8/10**. 143 commits |
| 11 avril 2026 | 1.3.3 | Stock Original (AdminStockOriginal, InventaireOriginal, grand livre, verrouillage trimestriel Refashion). Fix mobile (navigation incidents, checklist, erreurs silencieuses). Suppression doublon route `GET /api/vehicles/available`. Documentation : LOGIQUE_TOURNEES.md, LOGIQUE_STOCK_INVENTAIRES.md, VARIABLES_APPLICATION.md. 66 pages React, 63 fichiers routes. |
| 16 avril 2026 | 1.4.0 | **Module Boutiques complet** — Espace performance retail 2nde main textile. 9 tables (boutiques, import_batches, ventes, tickets, commandes, commande_lignes, commande_historique, objectifs, meteo_quotidien). 5 routes API (boutiques, boutique-ventes, boutique-commandes, boutique-objectifs, boutique-meteo). 7 pages React (Hub, Dashboard 3 niveaux jour/mois/année, Ventes, Commandes Kanban 3 colonnes, Planning, Objectifs, Import). Utilitaire partagé `utils/weather.js` (Open-Meteo, WMO). Nouveau rôle `RESP_BTQ`. Import CSV LogicS (séparateur `;`, SHA-256 anti-doublon, reconstruction tickets par minute). Commandes par lot/poids (5 statuts : brouillon→envoyee→ajustee→en_preparation→expediee + annulee). Corrélation météo/CA. Objectifs mensuels avec % atteinte. 2 jobs scheduler (scan CSV auto + collecte météo). Sidebar section Boutiques (pink). Docker volume CSV. Seed St-Sever + L'Hopital. 21 fichiers, +3 298 lignes. 75 pages React, 69 fichiers routes. |
| 21 avril 2026 | 1.4.0 | Audit & consolidation des branches : 7 branches distantes analysées, 6 sans écart avec main (déjà intégrées ou obsolètes), 1 branche (`claude/inventory-production-workflow-wzNrD`) avec 1 commit unique `a824d01`. Refonte BalancePage obsolète (remplacée sur main par d840360 + 3d0cefa). **Fix vehicles.js récupéré** : null-safe typage `team_id` (parseInt) et `tare_weight_kg` (parseFloat), champs optionnels en null explicite pour éviter "invalid input syntax for type integer" PostgreSQL. Branches obsolètes à nettoyer : add-karpathy-skills-plugin, document-mobile-behavior, fix-pcm-engine-questions, inventory-production-workflow, redesign-layout-kpi, shop-performance-management, update-collection-tracking (toutes déjà fusionnées sur main). |
| 22 avril 2026 | 1.4.1 | **Boutiques — nouveau format CSV LogicS + KPIs retail enrichis.** CSV LogicS à 11 colonnes avec `Num_Ticket` réel (un fichier par boutique par jour, ex : `LogicS_Ventes_20260421.csv`). Migration DB : ajout `num_ticket VARCHAR(32)` sur `boutique_tickets` et `boutique_ventes` (index partiels). Import auto-détecte le format (10 ou 11 col) et privilégie le vrai ticket LogicS — les tickets chevauchant la même minute sont désormais distingués. 3 nouveaux endpoints analytics : `/kpis` (CA TTC/HT, TVA collectée, panier moyen, IPT, prix moyen article, part promo CA/volume, durée moyenne ticket, taux attache sac), `/hourly` (fréquentation par heure + heatmap jour-semaine × heure), `/evolution` (comparaison période vs période équivalente N-1 avec deltas %). Dashboard boutique enrichi : 8 cartes KPI retail sur vues Jour & Mois avec badges de variation (± vs J-1 / M-1), heatmap de fréquentation hebdomadaire, graphique combiné CA/tickets par heure. Composant `KpiCard` étendu avec prop `footer`. Rétrocompatible ancien format CSV. |
| 28 avril 2026 | 1.4.2 | **Consolidation branches + récupération de fixes** : audit des 14 branches distantes (10 obsolètes 0 commit en avant, 4 avec écarts). Merge de 3 branches sur main : (1) **`ecstatic-darwin`** — audit-A sécurité : switch/case anti-injection SQL `admin-db.js`, suppression `detail: err.message` × 3 (`insertion/routes.js`), séparation `PCM_ENCRYPTION_KEY`/`JWT_SECRET`, 7 index FK manquants (`stock_movements`, `gps_positions`, `incidents`, `tonnage_history`), NOT NULL FK critiques (`tours`, `tour_cav`, `gps_positions`), `deploy.sh` exit 1 si init-db échoue. (2) **`setup-cav-data-retrieval`** — capteurs LoRaWAN Milesight EM400-MUD via Orange Live Objects (services API+MQTT+processor, décodeur, webhook, migration), pages AdminSensors+SensorSection, hook `useCavSensorSocket`, picker Live Objects, fix tiles **CARTO Basemaps** (corrige `Access blocked` OSM dû à Referrer-Policy). (3) **`merge-and-update-docs`** — null-safe typage `vehicles.js` (parseInt team_id, parseFloat tare_weight_kg). Branches obsolètes à supprimer côté remote (11 au total). |
| 1er mai 2026 | 1.5.0 | **Plan d'action multi-agents — 12 actions P0/P1 livrées en code + 9 runbooks**. Audit multi-agents (Architecte, Debug, Performance, UI, System Design) → 21 actions priorisées (note projet 5.4/10). **Code livré** : (1) fix bug bloquant **ProduitsFinis** (`catalogue_id`/`code_barre` alignés front/back, état d'erreur visible) ; (2) **fix N+1 dashboard objectifs** (1+N requêtes → 3 parallèles, −80 % latence estimée) ; (3) **throttle GPS proximité CAV** (cache `tour_cav` TTL 60 s + throttle 5 s, −98 % requêtes DB par tournée) ; (4) **9 index FK manquants** (`tour_cav`, `incidents`, `gps_positions(vehicle_id, recorded_at)`, `stock_movements.matiere_id`, `tour_weights`, `tours(driver_employee_id, date)`, `candidates.status`, `employees.insertion_status`) ; (5) routes **`/api/health` + `/live` + `/ready`** (timeouts DB 5 s / Redis 1.5 s, mémoire, status 200/503) ; (6) script **`backup-s3.sh`** (Scaleway Object Storage compatible, SHA-256, rétention différentielle daily/weekly/monthly/manual, alerting webhook) ; (7) **code splitting Vite** (`manualChunks` vendor-react/charts/maps/icons/net) + **React.lazy** sur 75 pages → bundle initial −60 % estimé ; (8) **Redis 128 MB→256 MB** (`allkeys-lru`, persistance) + middleware **`cacheMiddleware`** (header `X-Cache: HIT/MISS`, TTL 120 s sur `/dashboard/kpis` et `/dashboard/objectifs`) ; (9) composants UI partagés **`FormField`** (a11y `useId`, `aria-invalid`, `aria-describedby`, types text/email/number/date/select/textarea) et **`ErrorState`** (variantes inline/card, bouton Réessayer focus visible) ; (10) **Modal a11y** : focus trap Tab/Shift+Tab, focus restoration, `aria-labelledby`, contraste WCAG AA ; (11) **`:focus-visible`** global, classes `.skip-link` et `.sr-only` ; (12) middleware **`request-logger`** (UUID `x-request-id` propagé, structured JSON winston, durée, `user_id`, IP, niveau auto error/warn/info, catégorie `http_request_slow` au-delà de 1 s). **Runbooks** : `docs/RUNBOOK_INFRA_ROADMAP.md` couvre activation backups S3, refactor `tours/` (15 → 3 fichiers), `BillingService` + fusion facturation, réplication PostgreSQL, repository layer 5 modules, `ModalForm`/`DataGrid`, scheduler dédié + BullMQ workers, secrets manager Scaleway/Vault, vues matérialisées dashboard, pagination/virtualisation. **Rapport** : `rapports/rapport-implementation-2026-05-01.md`. |
| 2 mai 2026 | 1.5.1 | **Hotfixes prod post-déploiement V1.5.0** : (1) `[DASHBOARD] column "status" does not exist` — la requête `parcoursInsertionActifs` cherchait `insertion_diagnostics.status` qui n'existe pas dans le schéma. Bug pré-existant masqué par catch silencieux + cache Redis. Fix : utilise `employees.insertion_status = 'en_parcours'` (vraie source) ; (2) `duplicate key constraint employees_pkey` à la conversion candidat → employé : séquence `employees_id_seq` désynchronisée (cas typique d'import avec IDs explicites). Fix : resync auto idempotent de 50+ séquences SERIAL critiques au démarrage du backend (DO block PL/pgSQL) ; (3) Redis `maxmemory-policy` : `allkeys-lru` → `volatile-lru` → `noeviction` (exigence stricte BullMQ string match). Le cache fonctionne car les clés `cache:*` ont un TTL natif de 120s (expiration sans LRU). |
| 2 mai 2026 | 1.7.0 | **Vague V6 — Patterns architecturaux pilotes** (3 PRs). (1) **V6.1 state machine sur `commandes-exutoires`** : workflow le plus complexe du module logistique (9 statuts, 8 transitions valides) migré vers le moteur centralisé `services/state-machine`. `PATCH /:id/statut` consomme désormais `stateMachine.transition()` au lieu d'une table inline ; verrou pessimiste `FOR UPDATE` ajouté pour anti-race condition concurrente ; statut `chargee` réinjecté dans le validator (oubli historique signalé par les tests E2E) ; audit double `commandes_exutoires_historique` (métier riche, dans la TX) + `state_transitions_audit` (transverse, post-commit). (2) **V6.2 extraction `TourService`** : le hot path Socket.IO `gps:position` dupliquait sa requête `SELECT FROM tour_cav JOIN cav` avec 4+ fichiers de `routes/tours/`, et avait sa propre fonction haversine inline. Service partagé `services/TourService.js` (3 fonctions) : `haversineDistance` (garde-fou `isFinite`), `loadTourCAVs(pool, tourId)`, `findCAVsInProximity(lat, lng, cavs, radiusKm=0.1)` — destiné aux usages transverses (proximité GPS temps réel, scheduler, dashboards). `routes/tours/geo.js` reste source pour la logique TSP/OSRM. Smoke tests manuels : Paris→Rouen ≈112.1km, NaN→0, filtre 100m sur 3 CAVs OK. (3) **V6.3 pilote pattern Repository** — `InvoiceRepository` : extrait toutes les requêtes SQL du domaine facturation interne (`routes/billing.js`) dans `repositories/InvoiceRepository.js`. 6 méthodes (`findAll`, `findById`, `findLinesByInvoiceId`, `findByIdWithLines`, `create`, `updateStatus`) acceptent un argument optionnel `client` permettant l'utilisation depuis l'intérieur d'une transaction. `billing.js` devient thin controller pur (toute la SQL inline a disparu). 13 tests Jest unitaires sur le repository (mock `pg`) — 0 régression sur 139 tests existants. Pattern destiné à être répliqué en V6.5+ : `EmployeeRepository`, `CommandeExutoireRepository`, `FactureExutoireRepository`. **V6.4 abandonné après évaluation** (migration `useAsyncData` sur LiveVehicles / FillRateMap / Production) : ROI négatif — LiveVehicles + FillRateMap ont leur state piloté par Socket.IO (incompatible avec un hook fetch one-shot), Production n'a pas d'état `loading`/`error` à simplifier (la migration ajouterait une feature au lieu de réduire le code). |
| 2 mai 2026 | 1.6.0 | **Vague V5 — Backlog d'amélioration progressive** (4 PRs). (1) **V5.1 useAsyncData adoption** : Tours.jsx + Employees.jsx migrés vers le pattern `useAsyncData` + `ErrorState` (livrés en V3.3). Tours = 1 fetcher simple + bouton Réessayer. Employees = 3 fetchers parallèles dans une même promise + reload auto sur changement de filtre. 5 autres pages (LiveVehicles, FillRateMap, Production, Candidates, BoutiquesDashboard) gardées pour adoption manuelle (Socket.IO temps réel ou state local complexe). (2) **V5.2 xlsx → exceljs** : élimine vuln HIGH `xlsx` (Prototype Pollution + ReDoS sans fix amont). Wrapper de compatibilité `backend/src/utils/xlsx-compat.js` qui imite l'API xlsx en surface (`readFile` + `utils.sheet_to_json`) en utilisant exceljs en interne. 5 scripts migrés sans réécriture (juste require + await). xlsx désinstallé du package.json. Vuln backend : 4 → 3 moderate (anthropic-ai/sdk + uuid via exceljs restent). (3) **V5.3 state machine pilote** : `boutique-commandes.js` migré vers le moteur centralisé livré en V2.3. Suppression de `TRANSITIONS` locale, validation via `stateMachine.canTransition()` avec vérification de rôle native. Audit double : `boutique_commande_historique` (métier riche, dans la TX) + `state_transitions_audit` (transverse, post-commit best effort). Pattern validé pour extension aux 4 autres routes statutaires (commandes-exutoires, preparations, controles-pesee, factures-exutoires). (4) **V5.4 BillingService** : extraction logique partageable entre `billing.js` et `factures-exutoires.js`. Service `services/BillingService.js` avec `generateInvoiceNumber(pool, prefix, table, column)` (garde-fou anti-injection regex SAFE), `calculateTotals(lines, tvaRate)` (arrondi centime, quantité/prix par défaut), `canTransitionStatus(from, to)` (state machine simple draft→sent→paid|overdue|cancelled). Refactor `billing.js` thin controller : POST en transaction explicite, PUT status valide la transition (avant : juste enum). 18 tests Jest verts. Backlog V6 : migration `factures-exutoires.js` + repository pattern. |
| 15 mai 2026 | 2.1.0 | **Module Vente au Kilo (VAK) — intégration SumUp directe + écran TV live**. Nouveau 27e module. Une VAK = un événement mensuel de 2-3 jours au siège (Rouen 49.4231 / 1.0993) encaissé sur caisse SumUp depuis novembre 2026. **6 tables** créées (idempotent dans `init-db.js`) : `vaks` (sessions explicites avec libellé / dates / objectifs CA TTC + poids kg / lieu / lat-lng), `vak_import_batches` (anti-doublon SHA-256), `vak_tickets` (clé naturelle `sumup_transaction_id` + `ref_transaction` lisible + `entry_mode` CHIP/CONTACTLESS/MAGSTRIPE), `vak_ventes` (lignes brutes avec `unite` kg/pce, `segment` textile_vrac/chaussures/consommables, `source` api_sumup/webhook_sumup/csv_manuel), `vak_meteo_quotidien`, `vak_sumup_sync_log`. **Service `backend/src/services/sumup.js`** centralise : chiffrement AES-256-GCM (réutilise `PCM_ENCRYPTION_KEY` ou clé dédiée `SUMUP_ENCRYPTION_KEY`), flow OAuth 2.0 (authorize → token → refresh auto à T-1min via scheduler), `GET /v0.1/me/transactions/history` paginé incrémental (cursor = dernier `newest_time` réussi), validation HMAC SHA-256 webhook (`Sumup-Signature`), parser CSV 17 colonnes (date FR `"15 mai 2026 10:15"`, décimales virgule, mapping description → segment, agrégation par `Réf. transaction`), capture météo Open-Meteo par VAK, émission Socket.IO `vak:live:transaction` + `vak:live:counters` après chaque ingestion. **Route `backend/src/routes/vak.js`** expose : VAK CRUD, analytics (`/kpis` CA/poids/€-par-kg/IPT/mix paiements, `/hourly`, `/segments`, `/payments`, `/comparison` vs N-1 + vs YTD), vues annuelles (`/annual/overview`, `/annual/hourly-heatmap`, `/annual/segments-trend`, `/annual/payment-mix`), `/live/current` (auto-detect VAK en cours), fallback CSV (`/:id/import-csv` + `/csv-batches`), et la suite SumUp (`/sumup/{status,credentials,auth-url,callback,sync,disconnect,webhook,sync-log,register-webhook}`). Webhook **monté avant** `authenticate` (public, signé HMAC) ; `index.js` capture `req.rawBody` via `express.json({verify})` pour la vérification HMAC. **Scheduler** ajoute 2 jobs : `syncVakSumUp` (catch-up horaire pendant une VAK active, sinon daily 3h) + `captureVakWeather` (J-1 → J+7). **5 pages React** : `VakPerformance` (sélecteur VAK, KPI cards avec deltas vs N-1, ComposedChart horaire CA/tickets/poids, heatmap jour×heure, PieChart segments + paiements, météo des jours, tableau comparaison), `VakAnnuel` (AreaChart segments stacké, LineChart % CB vs espèces, BarChart CA vs objectif par VAK, heatmap horaire annuelle, tableau récap €/kg), `VakSessions` (CRUD + onglet import CSV), `VakLive` (**kiosk plein écran**, gradient orange/slate, 3 BigCounters animés easeOutCubic, jauges objectif, ticker temps réel via Socket.IO `vak:join`, refresh chart horaire à chaque vente, écran fallback « Prochaine VAK » si hors période), `VakSumupConfig` (form credentials chiffrés, runbook OAuth 6 étapes avec redirect URI copiable, statut/sync/disconnect/register-webhook, log 20 dernières syncs). **Sidebar** : nouvelle section `Vente au Kilo` (icône `Scale`, couleur orange) ADMIN/MANAGER avec 4 entrées + sous-menu Réglages → Config SumUp (ADMIN). **Routes App.jsx** : `/vak`, `/vak/annuel`, `/vak/sessions`, `/vak/live`, `/admin/vak/sumup-config`. **Pas de credentials dans `.env`** — saisis dans l'UI, stockés chiffrés ; `.env.example` documente seulement `SUMUP_REDIRECT_URI` + `PUBLIC_BASE_URL`. **Indicateurs livrés** : CA TTC/HT, poids kg, prix moyen €/kg, IPT, panier moyen, part chaussures, mix paiement CB/espèces (% volume + % CA), part promo via colonne `remise`. **Indicateurs roadmap** : taux d'écoulement (tonnage approvisionné), CA/heure travaillée, taux de conversion (compteur visiteurs), mix entry_mode contactless. |
| 2 juillet 2026 | 2.1.1 | **Fix prod — certificat SSL expiré (`NET::ERR_CERT_DATE_INVALID`)**. Cause racine : certbot renouvelait bien sur le disque, mais nginx servait la copie statique du certificat faite au démarrage du conteneur (l'entrypoint inline de `docker-compose.prod.yml` copiait `live/solidata.online-0001/` → `live/solidata.online/` une seule fois) et n'était jamais rechargé → expiration ~90 j après le dernier restart nginx. Correctif : (1) entrypoint extrait dans `deploy/nginx/nginx-entrypoint.sh` avec boucle de fond qui re-copie le certificat renouvelé + `nginx -s reload` toutes les 6 h ; (2) nouveau script `deploy/scripts/reload-nginx-certs.sh` (re-copie + `nginx -t && nginx -s reload` sans coupure) ; (3) `deploy/crontab.txt` enchaîne le reload après chaque `certbot renew` (3h/15h) ; (4) runbook « Cas 4 — certificat expiré » ajouté à `deploy/DEPLOIEMENT.md`. Action serveur : `git pull` + `docker compose -f docker-compose.prod.yml run --rm certbot renew` + `docker compose -f docker-compose.prod.yml up -d --force-recreate nginx` + réinstaller le cron (`crontab deploy/crontab.txt`). |
| 11 mai 2026 | 2.0.5 | **Infra — câblage VAPID + PCM_ENCRYPTION_KEY dans docker-compose.prod.yml**. Le composant `NotificationBell` affichait « Push non configuré côté serveur » parce que `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` n'étaient pas passées du `.env` hôte au container backend (oubli historique — `services/push-notifications.js` les attend, mais elles arrivaient toujours `null` car non listées dans la section `environment:` du compose). Ajout aussi de `PCM_ENCRYPTION_KEY` (audit V1.4.2 demandait la séparation d'avec JWT_SECRET — le code a un fallback sur JWT_SECRET mais la séparation propre n'était pas effective faute de var câblée). Action prod requise : générer les VAPID keys côté serveur via `docker compose -f docker-compose.prod.yml exec backend npx web-push generate-vapid-keys`, coller les 2 clés dans `/opt/solidata.online/.env`, restart backend. |
| 11 mai 2026 | 2.0.4 | **CI/CD — Smoke test endpoints critiques hooké dans deploy.sh**. Réécriture de `scripts/tests/api-smoke.js` pour couvrir ~40 endpoints critiques groupés en 13 sections : santé, auth, dashboard, production, reporting, collecte, stock/tri, logistique exutoires, RH/insertion, finance, boutiques, référentiels, sécurité. Helper `checkEndpoint(id, path, desc, {authH})` qui considère 2xx/401/403 = OK (endpoint existe et fonctionne) et 5xx/404 = fail. Hook dans `deploy/scripts/deploy.sh` (étape 7/7) : après init-db et health check basique, lance le smoke. Si un endpoint 5xx → `error` qui arrête le déploiement et suggère le rollback via `git reset --hard HEAD~1`. Lit `API_USER` / `API_PASSWORD` depuis `.env` (variables ajoutées à `.env.example`). Mode dégradé (warning sans fail) si credentials absents — couvre seulement les endpoints publics. Mode strict `SMOKE_STRICT=true` pour traiter les SKIP comme des fails (utile en CI). Motivé par l'incident 11/05 où 3 bugs SQL (`v.registration_number`, `u.nom/u.prenom`, `positions.name`) étaient invisibles tant que le cache Redis tenait. |
| 11 mai 2026 | 2.0.3 | **Sécurité — Tier 1 bundle T1.1 + T1.2 + T1.7**. (T1.1 — chaîne stored XSS) Nouveau module mutualisé `backend/src/utils/upload-filters.js` exposant `imageFilter`, `csvFilter`, `documentFilter` qui valident à la fois extension et MIME (whitelist double, défense en profondeur). Appliqué aux 3 multer qui n'avaient aucun filtre : `boutique-ventes.js` (csvFilter), `employees.js` (imageFilter), `tours/index.js` (imageFilter). En complément, `app.use('/uploads', express.static(...))` remplacé par un handler custom (`backend/src/index.js`) qui : (a) résout le chemin de façon safe contre path-traversal (`..`), (b) refuse 415 si l'extension n'est pas dans la whitelist `UPLOAD_MIME_BY_EXT`, (c) force le Content-Type depuis la whitelist (plus de sniffing du contenu), (d) ajoute `X-Content-Type-Options: nosniff`, (e) ajoute `Content-Security-Policy: default-src 'none'; sandbox; frame-ancestors 'none'` sur la réponse — même si un fichier HTML/SVG arrivait à se loger ici, il ne pourrait ni exécuter de script ni charger de ressource externe. (T1.2 — CSP) Retrait de la directive script unsafe dynamique des 2 vhosts dans `deploy/nginx/conf.d/solidata.conf`. Smoke test obligatoire après deploy sur les pages avec Recharts (Dashboard, Production, BoutiquesDashboard) + Leaflet (CAVMap, LiveVehicles, FillRateMap) — si une lib casse, fallback temporaire à acter en revue. (T1.7 — `/health` ai-agent) Supprime le leak `jsonify({"status": "error", "detail": str(e)})` (DSN, host, credentials, stack potentiellement exposés en public) → renvoie juste `{"status": "error"}` et log le détail côté serveur via `logger.error(..., exc_info=True)`. |
| 10 mai 2026 | 2.0.2 | **Sécurité — purge secrets hardcodés (audit Tier 0 S1/S2/S4)**. (S1) Remplacement de la vraie valeur `DB_PASSWORD=solidata_secure_password_2026` dans `.env.example` racine par `CHANGE_ME_GENERATE_STRONG_PASSWORD` + commentaire mode d'emploi. (S2) Idem pour `JWT_SECRET=solidata-jwt-secret-change-in-production` → `CHANGE_ME_GENERATE_STRONG_SECRET`. Suppression des fallbacks hardcodés `JWT_SECRET="solidata-jwt-secret"` et `SECRET_KEY="solidata-agent-dev-key"` dans `ai-agent/app.py` au profit d'un helper `_require_secret(name, hint)` qui raise au démarrage si la variable est absente/vide (fail-fast explicite, plus de défaut « dev par défaut » qui finit par fuiter en prod). Idem dans `ai-agent/docker-compose.yml` : suppression des `:-solidata-jwt-secret` / `:-solidata-agent-secret`, les vars passent en bare `${JWT_SECRET}` / `${SECRET_KEY}`. (S4) Refonte du guard de `/dev/token` (`ai-agent/app.py`) : l'ancien check `os.environ.get("FLASK_ENV") == "production"` était bypassable (valeurs "prod", "staging", var absente ouvraient l'endpoint). Remplacé par un opt-in explicite `ENABLE_DEV_TOKEN` (valeurs "true"/"1"/"yes" pour activer, tout le reste → endpoint absent au moment de l'enregistrement de la route, donc 404 et non 403). Log warning quand activé. **Rotation prod requise** : `DB_PASSWORD` + `JWT_SECRET` à régénérer côté serveur Scaleway (`/opt/solidata.online/.env`) — la rotation `JWT_SECRET` invalide tous les JWT en cours (re-login utilisateurs web, re-auth automatique chauffeurs via raccourci home screen). Tests `ai-agent/tests/test_app.py` mis à jour pour fournir `SECRET_KEY` + `ENABLE_DEV_TOKEN=true` au setup (13/13 verts). |
| 10 mai 2026 | 2.0.1 | **Sécurité — auth chauffeur « 1 URL = 1 véhicule »**. Suppression du trou d'énumération du `POST /api/auth/driver-start` (qui acceptait un `vehicle_id` entier énumérable). Désormais l'endpoint accepte un `vehicle_token` (hex 32 char, espace 2¹²⁸). DB : `ALTER TABLE vehicles ADD COLUMN qr_token VARCHAR(32) UNIQUE NOT NULL DEFAULT encode(gen_random_bytes(16), 'hex')` + backfill auto idempotent. Backend : `GET /api/vehicles/:id/access-info` (ADMIN) renvoie l'URL d'accès courante ; `POST /api/vehicles/:id/regenerate-token` (ADMIN) régénère + révoque immédiatement le précédent raccourci ; `qr_token` strippé des routes `GET /` et `GET /:id` + retours `RETURNING *` des POST/PUT/assign-driver (defense in depth). Mobile : nouvelle route `/v/:token` (`VehicleLogin.jsx`) qui POST le token, stocke le JWT, nettoie l'URL via `history.replaceState`, et redirige sur `/checklist` ou `/vehicle-select` selon tournée du jour. L'ancienne `Login.jsx` (liste publique des véhicules dispo, source de l'énumération) devient une landing « pas d'accès — demande à ton responsable de paramétrer ton téléphone ». Admin web : `<VehicleAccessPanel>` ajouté dans l'onglet Détail véhicule avec URL + bouton Copier + bouton Régénérer + mode d'emploi 3 étapes (« Ouvre l'URL → Ajouter à l'écran d'accueil → Nomme le raccourci »). Nginx : `log_format main` enrichi d'un `map` qui transforme `/v/<hex32>` → `/v/REDACTED` dans les access logs (defense in depth contre fuite via aggregateurs). Distribution choisie : D3 (pairing en personne au dépôt) — pas de QR sticker, pas de SMS, pas de fallback véhicule de remplacement (le manager re-paramètre le téléphone si swap). Env var `MOBILE_BASE_URL` (défaut `https://m.solidata.online`). |
| 10 mai 2026 | 2.0.0 | **Vague Audit (3 sprints P0 → P1 → P2)** consolidant les exigences Refashion, Métropole de Rouen et QHSE. Préparée par 3 rapports d'audit métier rédigés en personas (`rapports/audits/2026-05-10-audit-{refashion,metropole-rouen,qhse}.md`). **Module Étiquettes carton** (avant la vague) : génération d'étiquettes A4 + sortie de stock par scan douchette HID. Nouvelles tables `postes_etiquetage`, `ref_dimensions(type, valeur, ordre)`, helper `base24` compatible Excel `BASE()` (formule legacy `="P"&poste&REPT("0",4-LEN(BASE(D7,24)))&BASE(D7,24)`). Pages `/tri/etiquettes` (workflow tactile 6 étapes + impression CODE128 via jsbarcode) et `/inventaire/sortie-cartons` (BTQ ou scan libre + beeps `AudioContext`). Auto-seed `produits_catalogue` depuis `backend/src/data/catalogue-base.json` (103 produits extraits du fichier source Excel `SAISIE PRODUIT.xlsm`). **Sprint P0 (5 chantiers)** : agrément Refashion exutoires (retiré ensuite, pas d'utilité) ; audit-trail DPAV (`created_by`/`updated_by`/`updated_at` + table `refashion_dpav_history` snapshot JSONB par INSERT/UPDATE) ; table `refashion_taux_subvention` versionnée par convention/avenant (tarif unique €/t entrant, clôture auto du précédent à l'ajout d'un nouveau) ; table `referentiel_communes(code_insee PK)` + `cav.code_insee_commune` FK ; 3 KPIs Métropole exposés (`/metropole/sortie-dynamique`, `/service-cav`, `/captation-par-commune`). **Sprint P1 (4 chantiers)** : refonte `categories_sortantes` alignée Dashboard 2026 (17 catégories + Refus de tri obligatoire + colonne `famille_refashion` enum reutilisation/recyclage/csr/elimination/retour + ordre) ; `operation_outputs.created_by` + `tour_cav.skip_reason` enum ; **5 vues SQL d'audit** (`vw_tonnage_annuel_tournee`, `vw_dpav_sortants`, `vw_dpav_communes`, `vw_subvention_refashion_mensuelle`, `vw_coherence_tri_filiere`) exposées via `GET /api/refashion/exports/:slug?format=csv` ; page `/admin/refashion-exports` (QHSE permanent) ; 3 endpoints KPI RH (`/employees/kpi/formation`, `/kpi/etp` base 1607 h/an, `/kpi/absenteisme`) affichés dans `/reporting-rh`. **Sprint P2 (4 chantiers)** : DROP `flux_sortants` orphelin ; UI `skip_reason` exposé dans Tours (badge ambre + tooltip) ; ALTER `insertion_milestones.sortie_employeur_siret` + `sortie_duree_contrat_mois` pour reporting DREETS ; calcul CO2 évité utilise désormais le **mix observé** depuis les colisages scellés (via `famille_refashion`) avec fallback hardcodé 40/35/15/10 si <100 kg. **Communes Métropole** : endpoint `POST /api/communes/refresh-metropole` qui appelle `geo.api.gouv.fr/epcis/200023414/communes` et upserte (remplace l'import CSV manuel par un bouton « Actualiser depuis API INSEE »). **Refonte sidebar Admin** : fusion Référentiels + Catalogue produits → entrée unique `/admin/catalogue` (8 tabs initialement, finalement 6 — associations et exutoires retirés car gérés ailleurs : `/admin-associations` collecte + `/exutoires-clients` commercial). Suppression doublons `Configuration Pennylane` (déjà dans `/settings`) et `Utilisateurs` (déjà dans sous-menu `Utilisateurs & RGPD`). Suppression page `Referentiels.jsx` (redirect 301 vers `/admin/catalogue`). Suppression table `associations` (DROP CASCADE, doublon avec `association_points`). **Refonte gammes** : suppression effective (DELETE) des anciennes (`BTQ EXTRA`, `BTQ STAND`, `CHIF`, `Pvak`) au profit de 4 nouvelles (`EXTRA`, `STANDARD`, `VAK`, `EXPORT`). **Fix endpoint `/api/health`** : enrichi avec `database.connected`/`postgis`/`version` + `modules.auth` pour que `/settings` affiche les bons statuts (auparavant tous KO car champs inexistants dans la réponse). **Perf Dockerfile backend** : suppression du `chown -R /app` final (~3 min → <30 s) en créant le user `appuser:1001` avant `npm ci --omit=dev` (qui s'exécute donc déjà au bon owner). **Doc** : 3 rapports d'audit + rapport de suivi consolidé `2026-05-10-audit-suivi-sprints.md`. |
| 3 mai 2026 | 1.8.0 | **Refonte UX globale + Contrôle facturation Pennylane**. (A) **Refonte UX (4 phases sur 1 PR)** : 10 quick wins UI (carte fond gris factorisée `MapSizeFix`, renommages /tours + /exutoires-gantt, fix Modal `isOpen` boutiques/commandes, widget actualités home, suppression /expeditions) ; refonte sidebar récursive 4 niveaux ; scission Pennylane (vue compta `/pennylane` + config technique `/admin/pennylane-config`) ; suppression de 8 hubs ; 8 refontes UI moyennes (entonnoir RH horizontal, graphe absentéisme, prévision 15J métropole, finance histos+courbes, trésorerie revenus/dépenses, filtres cliquables boutiques) ; 3 refontes métier (Production avec objectifs trim/mois + alerte CSR + clôture journée + listes managers, Collecte en direct multi-tournées avec endpoint `/tours/active-summary` + 12 couleurs, Tours détail enrichi via `/live-summary`). Import CSV Malibou (12 colonnes, séparateur `;`). (B) **Contrôle facturation** (nouveau sous `Opérations > Logistique > Commercial > Contrôle facturation`) : **outil de réconciliation, pas de génération**. Flux Pennylane PUSH retiré (POST /sync/invoices supprimé). Nouveau flux PULL incrémental `POST /api/pennylane/sync/customer-invoices` (filtre `since=last_sync_at`) qui importe les factures clients émises sur Pennylane et tente un matching auto sur `external_reference`/libellé → `commandes_exutoires.reference`. Endpoints de rapprochement manuel : `GET /factures-exutoires/stats`, `GET /candidates-for/:id`, `GET /orphan-commandes`, `POST /:id/link-commande`, `POST /:id/unlink` (ADMIN), `POST /:id/validate-ecart`. Comparaison automatique : quantité facturée Pennylane vs `controles_pesee.pesee_client` → `ecart_quantite_pct` (alerte non-bloquante au-delà de la tolérance globale `facturation_tolerance_pct` dans settings, défaut 5%). Bascule automatique de la commande en statut `cloturee` au rapprochement. Table `factures_exutoires` étendue (12 colonnes Pennylane + index unique anti-doublon) ; nouveau CHECK statut élargi (`recue`/`conforme`/`ecart`/`validee`/`rapprochement_manuel`/`ecart_valide`). Ancienne page `/exutoires-facturation` (OCR PDF jamais finalisé) supprimée — module multer/pdf-parse non chargé. |

---

## 13. CHECKLIST AVANT COMMIT

Avant de pousser du code :
1. Le code respecte les patterns existants (routes, pages, middleware)
2. Les nouvelles tables utilisent `CREATE TABLE IF NOT EXISTS`
3. Les routes sensibles ont `authenticate` + `authorize`
4. Les requêtes SQL sont paramétrisées ($1, $2...)
5. L'interface est en français
6. Le build Docker fonctionne (`docker compose build`)
7. Pas de secrets dans le code (utiliser .env)
8. La documentation est mise à jour si nouveau module

---

*Ce fichier est maintenu à jour à chaque évolution majeure du projet.*
*Pour contribuer : consulter DOCUMENTATION_TECHNIQUE.md pour le détail complet.*
